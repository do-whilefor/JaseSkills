# Runtime Evidence Orchestration Rules

A browser replay plan is not evidence. Ready runtime evidence requires HAR, trace, screenshots, request/response metadata, role/tenant mapping, and a redacted evidence manifest. GraphQL and WebSocket runtime replay must be local or explicitly allowlisted and non-destructive.
