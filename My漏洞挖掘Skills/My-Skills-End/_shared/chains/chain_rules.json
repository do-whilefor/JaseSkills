[
  {
    "id": "CH01",
    "title": "信息泄露 → 认证绕过",
    "required_evidence_nodes": [
      "sensitive_info_disclosure",
      "auth_bypass"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 2,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH02",
    "title": "source map → 隐藏接口 → IDOR",
    "required_evidence_nodes": [
      "sourcemap_endpoint",
      "hidden_route",
      "idor_bola"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 3,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH03",
    "title": "前端权限判断 → 后端鉴权缺失",
    "required_evidence_nodes": [
      "frontend_guard",
      "backend_guard_missing"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 2,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH04",
    "title": "文件上传 → 路径穿越 → 任意写入",
    "required_evidence_nodes": [
      "file_upload",
      "path_traversal",
      "arbitrary_file_write"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 3,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH05",
    "title": "任意写入 → 模板覆盖 → RCE 候选",
    "required_evidence_nodes": [
      "arbitrary_file_write",
      "template_override",
      "rce_candidate"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 3,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH06",
    "title": "SSRF → 内网 metadata 风险候选",
    "required_evidence_nodes": [
      "ssrf_candidate",
      "metadata_boundary_blocked"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 2,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH07",
    "title": "低权限接口 → role 修改 → 管理员提升",
    "required_evidence_nodes": [
      "low_privilege_endpoint",
      "role_update",
      "admin_escalation"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 3,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH08",
    "title": "邀请流程 → 租户加入 → 租户数据越权",
    "required_evidence_nodes": [
      "invite_flow",
      "tenant_join",
      "tenant_data_access"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 3,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH09",
    "title": "Webhook → SSRF → 敏感响应泄露",
    "required_evidence_nodes": [
      "webhook_fetch",
      "ssrf",
      "sensitive_response"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 3,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  },
  {
    "id": "CH10",
    "title": "依赖漏洞 → 项目可达调用点 → 实际影响",
    "required_evidence_nodes": [
      "dependency_cve",
      "reachable_callsite",
      "impact_evidence"
    ],
    "auto_confirm": false,
    "required_manifest_chain_nodes": 3,
    "rule": "每个节点必须有独立 evidence manifest 片段；链条可能性不得替代 confirmed。"
  }
]
