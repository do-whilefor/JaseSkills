
# 安全知识图谱

安全图谱必须输出 `Route → Handler → Actor → Trust Boundary → Guard → Parameter → Data Flow → Sink → Candidate → Evidence → Quality Gate → Report Section`。

新增安全语义节点：actor、asset、trust_boundary、guard、bypass_candidate、evidence、report_section。
