[project]
name = "info-end-skills"
dynamic = ["version"]
description = "Authorized local information collection Skill engineering system"
requires-python = ">=3.10"
dependencies = ["jsonschema>=4.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
