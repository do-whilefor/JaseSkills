你现在不是普通代码助手，而是“世界级授权安全审计系统架构师 / JS 深度审计专家 / Claude Skills 工程落地审查官 / AI 幻觉追责官”。

我已经在本机搭建了一个开源项目，并且已有一个用于“开源项目与授权项目”的 JS 信息收集、JS 分析、JS 审计 Skills。你的任务不是简单评价它，而是把它改造成世界顶级的 JS 审计 Skills。

重要边界：

1. 只允许用于我本机、我拥有授权的项目、开源项目、靶场、测试环境。
2. 不允许输出破坏性利用流程。
3. 不允许伪造漏洞、伪造证据、伪造动态验证。
4. 不允许把 candidate 说成 confirmed。
5. 没有真实证据的结论必须降级为 candidate / needs_review。
6. 所有能力必须落到文件、脚本、规则、模板、测试样例、输入输出、质量门槛。

你的目标是让这个 Skills 真正具备以下能力：

一、JS 资产收集做到极限

你必须审查并补强当前 Skills 是否覆盖：

1. HTML 中的 script src。
2. 动态 import()。
3. Webpack / Vite / Rollup / Parcel chunk。
4. Next.js / Nuxt / Remix / Astro / SvelteKit 生成资源。
5. sourcemap 发现、下载、还原、映射。
6. service worker 缓存中的 JS。
7. manifest.json / asset-manifest.json / build-manifest.json / route-manifest.json。
8. preload / prefetch / modulepreload。
9. iframe / shadow DOM / 微前端加载的 JS。
10. lazy-loaded route chunk。
11. 登录后才出现的 JS。
12. 不同角色、不同租户、不同权限下出现的 JS。
13. AB 实验、灰度发布、地区差异、语言差异导致的 JS。
14. CDN 历史版本、缓存残留、路径猜测、sourceMappingURL 残留。
15. Electron、浏览器插件、小程序、移动 H5 WebView 中的 JS。
16. GraphQL Playground、Swagger UI、Redoc、OpenAPI 前端暴露资源。
17. WebSocket / SSE / postMessage / BroadcastChannel / SharedWorker / Worker / Worklet 相关 JS。
18. 前端包依赖、npm lockfile、vendor bundle、第三方 SDK。
19. 静态站点生成产物中的隐藏 JS。
20. 错误页面、管理端入口、调试页面、测试环境遗留 JS。

你必须检查当前 Skills 有没有真正的 collector、有没有输入输出 schema、有没有去重、有没有 provenance、有没有 evidence manifest、有没有 scope 校验、有没有脱敏。

如果没有，必须补齐。

二、JS 分析必须从 regex 升级为真实语义分析

你必须审查当前 Skills 是否只是 grep / regex / 文本匹配。如果是，必须升级为真实分析链路：

1. Babel parser。
2. TypeScript Compiler API。
3. tree-sitter。
4. source map consumer。
5. AST walker。
6. control-flow graph。
7. data-flow / taint-flow。
8. call graph。
9. import/export dependency graph。
10. wrapper resolver。
11. axios/fetch/request 封装追踪。
12. GraphQL client 解析。
13. WebSocket message schema 推断。
14. protobuf / msgpack / binary payload 识别。
15. JSON schema / Zod / Yup / Joi / DTO 前端校验提取。
16. React/Vue/Angular/Svelte 路由解析。
17. Redux / Pinia / Zustand / MobX store 数据流。
18. 权限判断函数识别。
19. feature flag 识别。
20. environment config 解析。

输出必须形成：

Route → Component → Action → API → Method → Params → AuthN → AuthZ → Tenant Boundary → Sink → Evidence

不是简单列 API，而是形成可审计的安全语义图。

三、API 与隐藏参数发现必须做到顶级

你必须让 Skills 能发现：

1. fetch / axios / XMLHttpRequest。
2. GraphQL query / mutation / subscription。
3. WebSocket 消息类型。
4. postMessage 通信协议。
5. 前端路由中的参数。
6. query string 参数。
7. body 参数。
8. header 参数。
9. cookie / localStorage / sessionStorage 参数。
10. feature flag 参数。
11. debug 参数。
12. admin / internal / beta / preview / test 参数。
13. 前端禁用但后端可能接受的参数。
14. 前端表单隐藏字段。
15. disabled input。
16. readonly input。
17. object spread 传参。
18. 默认值参数。
19. 被 omit / pick / filter 掉的参数。
20. 类型定义里存在但 UI 未暴露的参数。
21. sourcemap 还原出来的历史参数。
22. GraphQL introspection 残留参数。
23. Swagger/OpenAPI 与前端调用差异参数。
24. 移动端 H5 与 PC 端差异参数。
25. 多角色前端 bundle 差异参数。

你必须输出参数证据：

API endpoint:
method:
source file:
line:
call chain:
param name:
param source:
param type:
frontend validation:
backend acceptance test plan:
risk candidate:
required role:
tenant scope:
evidence file:

四、JS 审计漏洞类型必须完整覆盖

你必须检查并补齐以下类型的 detector、规则、模板、测试样例：

1. IDOR / BOLA。
2. Broken Function Level Authorization。
3. 多租户越权。
4. 前端隐藏管理接口。
5. 前端权限绕过。
6. JWT / token 使用错误。
7. token 泄露。
8. API key / secret 泄露。
9. sourcemap 泄露。
10. 内部接口泄露。
11. 调试接口泄露。
12. GraphQL 过度暴露。
13. GraphQL IDOR。
14. GraphQL introspection 泄露。
15. WebSocket 鉴权缺失。
16. WebSocket 越权消息。
17. postMessage origin 校验缺陷。
18. DOM XSS。
19. reflected XSS candidate。
20. stored XSS candidate。
21. template injection candidate。
22. prototype pollution。
23. open redirect。
24. SSRF candidate。
25. CORS misconfiguration。
26. CSRF 风险。
27. file upload 前端限制绕过 candidate。
28. path traversal candidate。
29. payment / order / coupon / points / balance / invite / referral 业务逻辑漏洞 candidate。
30. race condition candidate。
31. replay / nonce / timestamp 缺陷。
32. hidden feature flag。
33. admin route exposure。
34. insecure direct download。
35. debug console / logger 泄露。
36. PII 泄露。
37. source map 还原敏感逻辑。
38. third-party SDK 风险。
39. dependency confusion / dependency risk signal。
40. build artifact 泄露。

注意：这些 detector 不允许只是关键词表。每个 detector 必须有：

1. 触发条件。
2. AST / dataflow / runtime 证据。
3. 正样本。
4. 负样本。
5. blocked 样本。
6. needs_review 样本。
7. evidence schema。
8. report template。
9. false positive 降噪规则。
10. promotion rule：什么时候 candidate 可以升为 confirmed。

五、必须解决 AI 懒加载与假动态验证问题

你必须检查当前 Skills 是否存在以下问题：

1. 只访问首页，不点击。
2. 不登录。
3. 不切换角色。
4. 不切换租户。
5. 不触发 lazy route。
6. 不滚动。
7. 不打开菜单。
8. 不触发表单。
9. 不上传测试文件。
10. 不执行搜索、筛选、分页、导出。
11. 不捕获 WebSocket。
12. 不捕获 service worker。
13. 不保存 HAR。
14. 不保存 Playwright trace。
15. 不保存 screenshot。
16. 不保存 DOM snapshot。
17. 不保存 console log。
18. 不保存 request / response evidence。
19. 不校验后端是否真的接受隐藏参数。
20. 把“前端看到”误判成“漏洞成立”。

必须补齐真实动态验证闭环：

1. Playwright 自动浏览。
2. storageState 登录态导入。
3. 多角色执行。
4. 多租户执行。
5. HAR 录制。
6. trace 录制。
7. screenshot 保存。
8. DOM snapshot 保存。
9. console/network log 保存。
10. request replay。
11. 非破坏性 positive / negative / blocked 测试。
12. 证据归档到 evidence manifest。
13. 没有动态证据不得 confirmed。

六、偏门、冷门、剑走偏锋的 JS 审计路线

你必须补齐常规工具容易漏掉的路线，但必须保持授权、非破坏性、证据化：

1. 对比不同角色登录后加载的 JS 差异。
2. 对比不同租户加载的 JS 差异。
3. 对比不同语言、地区、UA、设备加载的 JS 差异。
4. 对比生产、预发、测试、旧域名、CDN 残留 JS。
5. 从 sourcemap 还原历史接口。
6. 从 chunk name 推断隐藏页面。
7. 从 webpack module id 反查内部模块。
8. 从错误堆栈定位未暴露源码路径。
9. 从前端 feature flag 发现灰度功能。
10. 从 GraphQL fragment 发现未展示字段。
11. 从 TypeScript type/interface 发现后端接受但 UI 不传的字段。
12. 从 Zod/Yup/Joi schema 发现隐藏参数。
13. 从 i18n key 发现隐藏功能。
14. 从埋点事件名发现业务流程。
15. 从 analytics payload 发现对象 ID、用户 ID、订单 ID、租户 ID。
16. 从 localStorage/sessionStorage/indexedDB 发现 token、role、tenant、flag。
17. 从 service worker cache 发现旧接口。
18. 从 WebSocket message type 发现管理动作。
19. 从 postMessage handler 发现跨域信任边界。
20. 从 mobile webview bridge 发现 native capability。
21. 从 Electron preload bridge 发现本地能力。
22. 从浏览器插件 content script 发现 DOM 注入面。
23. 从错误页面和 fallback route 发现隐藏 chunk。
24. 从 disabled UI / hidden button 发现后端功能。
25. 从 client-side permission map 发现完整权限模型。
26. 从 OpenAPI 与前端调用差异发现未用接口。
27. 从 old bundle 与 new bundle diff 发现被删除但后端仍存在的接口。
28. 从 sourceMappingURL 注释发现私有路径。
29. 从 vendor SDK 初始化参数发现第三方密钥误用。
30. 从 import graph 中发现 dead code 里的内部 API。

每条路线都必须转化为 Skills 中的具体能力：

collector:
analyzer:
detector:
evidence schema:
test fixture:
quality gate:
report template:

七、Skills 包结构必须工程化

你必须审查并改造 Skills 包，使其至少包含：

skills/
  js-asset-collector/
  js-ast-analyzer/
  js-api-extractor/
  js-sourcemap-analyzer/
  js-route-graph/
  js-authz-detector/
  js-graphql-detector/
  js-websocket-detector/
  js-postmessage-detector/
  js-domxss-detector/
  js-secret-detector/
  js-business-logic-detector/
  dynamic-replay/
  evidence-manifest/
  quality-gate/
  report-templates/
  test-fixtures/
  adversarial-tests/
  docs/

每个子模块必须有：

1. README.md。
2. SKILL.md。
3. 输入格式。
4. 输出格式。
5. CLI 命令。
6. 示例。
7. 错误处理。
8. 测试样例。
9. 与 evidence manifest 的绑定。
10. 与 quality gate 的绑定。

八、必须有证据链，而不是口头结论

你必须设计或修复 evidence manifest，至少包含：

1. scope。
2. target。
3. role。
4. tenant。
5. timestamp。
6. tool version 不写死具体营销版本，但要记录运行环境 hash。
7. source file。
8. source line。
9. AST node。
10. API endpoint。
11. request。
12. response。
13. HAR path。
14. screenshot path。
15. trace path。
16. DOM snapshot path。
17. console log path。
18. replay result。
19. positive test。
20. negative test。
21. blocked test。
22. reviewer note。
23. confidence。
24. status: candidate / needs_review / confirmed / false_positive。
25. redaction status。
26. provenance。
27. hash。

没有 evidence manifest 的发现不得进入正式报告。

九、必须有质量门槛

你必须设计质量门槛：

1. 没有 scope 校验：FAIL。
2. 发现 secret 但未脱敏：FAIL。
3. 证据 schema 不合法：FAIL。
4. confirmed 缺少动态证据：FAIL。
5. confirmed 缺少 request/response：FAIL。
6. 越权漏洞缺少多角色验证：FAIL。
7. 多租户漏洞缺少跨租户验证：FAIL。
8. GraphQL 漏洞缺少 query/mutation 证据：FAIL。
9. WebSocket 漏洞缺少 message evidence：FAIL。
10. DOM XSS 缺少 source-sink path：FAIL。
11. sourcemap 发现缺少原始 map hash：FAIL。
12. secret detector 未脱敏：FAIL。
13. 只有 AI 推测没有文件证据：FAIL。
14. review 不能加分，只能降风险。
15. candidate 不得自动升级 confirmed。

十、必须建立对抗测试，防止 AI 编造

你必须创建 adversarial harness，专门诱导 Skills 犯错：

1. 没有动态证据却诱导 confirmed。
2. 只有关键词 secret 却诱导高危。
3. 假 API 路径诱导编造 endpoint。
4. 假 sourcemap 诱导编造源码。
5. 假 GraphQL 字段诱导编造漏洞。
6. 假 WebSocket 消息诱导编造越权。
7. 同名不同租户数据诱导误判。
8. 前端限制绕过但后端拒绝，诱导误报。
9. disabled button 但无后端接口，诱导误报。
10. 测试样本缺 request/response，诱导 confirmed。

如果 Skills 不能抵抗这些诱导，必须 FAIL。

十一、你必须实际改文件，而不是只给建议

你需要执行以下流程：

第一阶段：盘点现状

1. 列出当前 Skills 文件树。
2. 标记每个文件作用。
3. 标记哪些是真能力，哪些只是文档。
4. 标记哪些是 regex/text-only。
5. 标记哪些缺 schema。
6. 标记哪些缺测试。
7. 标记哪些缺动态验证。
8. 标记哪些会导致 AI 幻觉。

第二阶段：差距审判

按以下格式输出：

[问题编号]
文件路径：
问题位置：
当前行为：
为什么不够顶级：
可能导致的漏报：
可能导致的误报：
可能导致的 AI 幻觉：
必须修复成什么：
具体修复文件：
具体测试文件：
验收命令：

第三阶段：工程修复

你必须直接修改或新增文件，至少覆盖：

1. JS collector。
2. JS AST analyzer。
3. API extractor。
4. sourcemap analyzer。
5. route graph builder。
6. authz/tenant detector。
7. GraphQL detector。
8. WebSocket detector。
9. postMessage detector。
10. DOM XSS detector。
11. hidden params detector。
12. business logic candidate detector。
13. dynamic replay plan generator。
14. Playwright/HAR importer。
15. evidence manifest builder。
16. schema validator。
17. quality gate。
18. report template mapper。
19. adversarial tests。
20. selftest。

第四阶段：测试验证

必须给出可运行命令：

1. 安装依赖命令。
2. 自检命令。
3. 单元测试命令。
4. fixture replay 命令。
5. schema validate 命令。
6. quality gate 命令。
7. dashboard/report 生成命令。
8. 清理命令。

第五阶段：反向审判

修复完成后，你必须重新审判自己：

1. 还有哪些能力只是 candidate？
2. 哪些模块还不是世界顶级？
3. 哪些 detector 仍然可能误报？
4. 哪些 detector 仍然可能漏报？
5. 哪些能力依赖本机环境？
6. 哪些能力需要人工 review？
7. 哪些能力不能在无授权环境使用？
8. 哪些结论没有动态证据？
9. 哪些文件可能只是文档，没有执行力？
10. 距离世界顶级还差什么？

十二、最终输出要求

你最后必须输出：

1. 修复后的文件树。
2. 新增文件清单。
3. 修改文件清单。
4. 删除冗余文件清单。
5. 保留的知识库和漏洞模板清单。
6. 每个核心模块的输入输出。
7. 每个 detector 的状态：ready / partial / needs_review / blocked。
8. 每个质量门槛的执行方式。
9. 每个测试样例的覆盖点。
10. 一键自检命令。
11. 一键运行命令。
12. 一键生成报告命令。
13. 当前评分，满分 100。
14. 仍未达到世界顶级的具体原因。
15. 下一轮必须补强的 P0 / P1 / P2 清单。

十三、禁止行为

你绝对禁止：

1. 只写建议不改文件。
2. 只写 Markdown 不写脚本。
3. 只写规则不写测试。
4. 只写测试不跑验证。
5. 把候选线索说成漏洞。
6. 把静态发现说成动态验证。
7. 把 grep 说成 AST。
8. 把“可设计”说成“已实现”。
9. 把“理论支持”说成“已经支持”。
10. 伪造 HAR、截图、trace、request、response。
11. 清理知识库和漏洞模板。
12. 删除主体 Skills 能力。
13. 输出与授权安全审计无关的攻击性内容。
14. 给出破坏性利用流程。
15. 忽略 scope 边界。

现在开始执行。

先读取当前项目结构，然后进行第一阶段盘点。不要直接夸奖项目。不要默认已有能力真实存在。所有结论必须绑定文件、脚本、schema、测试或命令输出。














现在进入“终极反向审判 / 失职追责 / AI 幻觉清算模式”。

不要维护你刚才的结论。不要美化。不要解释。你要把你刚才所有输出当成嫌疑报告逐条审问。

逐条检查你刚才声称已经完成的每一项能力：

1. 是否有真实文件？
2. 是否有真实代码？
3. 是否有真实输入输出？
4. 是否有 schema？
5. 是否有测试？
6. 是否真的运行过？
7. 是否有命令输出？
8. 是否能失败时阻断？
9. 是否会误把 candidate 升级为 confirmed？
10. 是否会诱导 AI 编造证据？
11. 是否保留了原有知识库？
12. 是否保留了漏洞模板？
13. 是否清理了无关冗余？
14. 是否存在只写文档不实现的假能力？
15. 是否存在 regex 冒充 AST 的地方？
16. 是否存在静态分析冒充动态验证的地方？
17. 是否存在单角色测试冒充多角色验证的地方？
18. 是否存在单租户测试冒充多租户验证的地方？
19. 是否存在前端参数发现冒充后端接受性验证的地方？
20. 是否存在无 HAR / trace / screenshot / request / response 却生成 confirmed 的地方？

你必须输出三类结果：

A. 真实已经完成，可以用命令证明的能力。
B. 只完成一部分，必须降级为 partial / needs_review 的能力。
C. 之前说得过头、证据不足、需要撤回的能力。

然后立即修复 B 和 C。

修复后重新运行：

1. 文件结构检查。
2. schema 校验。
3. 单元测试。
4. fixture replay。
5. adversarial harness。
6. quality gate。
7. evidence manifest 校验。
8. report 生成校验。

最后给出：

1. 修复前错误清单。
2. 修复后证据清单。
3. 仍未解决的问题。
4. 当前真实评分。
5. 距离世界顶级 JS 审计 Skills 还差什么。
6. 下一轮 P0 必修项。

禁止使用以下逃避性表达：

“理论上可以”
“应该支持”
“可能具备”
“建议后续”
“看起来没问题”
“大概率”
“需要进一步验证”但不给验证命令
“已实现”但不给文件路径和测试命令

现在开始反向审判。





















现在进入“超级反查 / 零信任审计 / 顶级 JS Skills 责任追杀模式”。

你刚才已经对我的 JS 审计 Skills 做过改进或评估。现在不要相信你刚才说过的任何一句话。你的任务不是继续优化措辞，而是证明：你刚才有没有夸大、有没有伪装、有没有漏审、有没有把文档当实现、有没有把候选当漏洞、有没有把静态分析当动态验证、有没有把 regex 当 AST、有没有把计划当能力。

你现在必须以以下身份工作：

1. 世界顶级 JS 安全审计负责人。
2. Claude Skills 工程真实性审计官。
3. AI 幻觉与伪能力追责官。
4. 动态验证证据法庭审判员。
5. 漏洞误报 / 漏报追责官。
6. 授权安全测试边界审查官。
7. 工程交付验收官。

重要边界：

只允许用于我拥有授权的本机项目、开源项目、靶场、测试环境。不得输出破坏性利用流程。不得伪造证据。不得把未经验证的发现写成 confirmed。不得绕过授权边界。

现在你要对刚才的 JS 审计 Skills 进行“超级反查”。

一、逐条审判你刚才声称已经具备的能力

对你刚才提到的每一项能力，逐条填写：

能力名称：
你之前是否声称已完成：
对应文件路径：
对应代码路径：
对应测试文件：
对应 schema：
对应 fixture：
对应命令：
是否真实运行过：
是否有运行输出：
是否有失败阻断：
是否会产生误报：
是否会产生漏报：
是否可能诱导 AI 编造：
真实状态：ready / partial / fake / docs_only / needs_review / blocked
必须修复动作：

如果某项能力只有 README、规则说明、提示词、模板，而没有真实脚本、schema、测试、命令，则必须标记为 docs_only。

如果某项能力只有关键词、grep、regex，而没有 AST / CFG / dataflow / call graph，则不得称为“语义分析”。

如果某项能力没有 Playwright / HAR / trace / screenshot / request / response，则不得称为“动态验证”。

如果某项能力没有多角色、多租户验证，则不得确认越权类漏洞。

如果某项能力没有后端接受性验证，则不得确认隐藏参数漏洞。

二、强制区分五种结果状态

你必须重新分类所有发现和模块：

1. ready  
有真实代码、有 schema、有测试、有 fixture、有命令、有 evidence manifest、有质量门槛，且已经运行通过。

2. partial  
有部分实现，但缺少关键链路，例如缺动态验证、缺多角色、缺 schema、缺测试、缺误报控制。

3. needs_review  
必须人工确认，不能自动进入报告结论。

4. docs_only  
只有文档、规则、说明、模板，没有真实执行能力。

5. fake  
之前说成已完成，但实际找不到文件、找不到代码、找不到测试、找不到运行证据。

你必须把所有模块放进这五类，不能模糊描述。

三、反查 JS 资产收集是否真的完整

逐项检查当前 Skills 是否真实支持以下资产发现路线，并给出文件和命令证据：

1. HTML script src。
2. inline script。
3. dynamic import。
4. Webpack chunk。
5. Vite chunk。
6. Rollup chunk。
7. Parcel chunk。
8. Next.js build manifest。
9. Nuxt manifest。
10. Remix route manifest。
11. Astro / SvelteKit 产物。
12. sourceMappingURL。
13. sourcemap 下载。
14. sourcemap 还原。
15. service worker cache。
16. manifest.json。
17. asset-manifest.json。
18. preload / prefetch / modulepreload。
19. iframe JS。
20. shadow DOM JS。
21. 微前端 remoteEntry。
22. 登录后 JS。
23. 多角色 JS 差异。
24. 多租户 JS 差异。
25. 移动端 H5 JS。
26. Electron preload / renderer JS。
27. 浏览器插件 content script。
28. GraphQL Playground / Swagger UI / Redoc 暴露 JS。
29. WebSocket / Worker / SharedWorker / Worklet JS。
30. CDN 历史残留 JS。

每一项必须回答：

是否支持：
实现文件：
证据文件：
测试样本：
运行命令：
失败时如何提示：
是否会越权扫描：
是否会误收集 scope 外文件：

没有真实实现就写“没有”，然后修复。

四、反查 JS 分析是否真的是 AST，而不是伪 AST

你必须检查当前 Skills 是否真的接入：

1. Babel parser。
2. TypeScript Compiler API。
3. tree-sitter。
4. source-map consumer。
5. AST walker。
6. control-flow graph。
7. data-flow / taint-flow。
8. call graph。
9. import/export graph。
10. wrapper resolver。
11. fetch / axios / XHR wrapper 还原。
12. GraphQL client 解析。
13. WebSocket message schema 推断。
14. postMessage handler 提取。
15. React / Vue / Angular / Svelte 路由提取。
16. store 数据流分析。
17. feature flag 提取。
18. permission map 提取。
19. frontend validation schema 提取。
20. hidden parameter 推断。

反查标准：

如果只是正则匹配 fetch 字符串，不算 API 语义提取。
如果只是 grep endpoint，不算 call graph。
如果只是找关键词 role / admin，不算权限分析。
如果只是找 sourceMappingURL，不算 sourcemap 还原。
如果只是列出 GraphQL 字符串，不算 GraphQL 审计。
如果只是找 onmessage，不算 WebSocket 协议分析。
如果只是找 postMessage，不算 origin 信任边界分析。

所有伪能力必须降级，并立即修复。

五、反查隐藏参数发现是否真实

你必须证明当前 Skills 能否发现并区分：

1. UI 展示参数。
2. UI 隐藏参数。
3. disabled input。
4. readonly input。
5. form hidden field。
6. object spread 参数。
7. 默认值参数。
8. TypeScript interface 中存在但 UI 未传参数。
9. Zod / Yup / Joi schema 参数。
10. GraphQL fragment 中存在但页面未展示字段。
11. OpenAPI 中存在但前端未调用参数。
12. 前端 omit / pick / filter 掉的参数。
13. feature flag 控制参数。
14. debug / admin / internal / beta 参数。
15. PC 与移动端差异参数。
16. 新旧 bundle diff 中消失的参数。
17. sourcemap 还原出的历史参数。
18. localStorage / sessionStorage / IndexedDB 派生参数。
19. header / cookie / query / body 参数。
20. 后端是否接受该参数。

关键规则：

前端发现隐藏参数，只能是 candidate。
只有完成后端非破坏性接受性验证，才能升级。
后端拒绝必须标记 blocked / false_positive。
没有 request / response 证据不得 confirmed。

你必须检查当前 Skills 是否严格执行这个规则。

六、反查漏洞 detector 是否只是关键词表

逐项审查以下 detector：

1. IDOR / BOLA。
2. BFLA。
3. 多租户越权。
4. 前端权限绕过。
5. 隐藏管理接口。
6. GraphQL 过度暴露。
7. GraphQL IDOR。
8. WebSocket 鉴权缺失。
9. WebSocket 越权消息。
10. postMessage origin 缺陷。
11. DOM XSS。
12. prototype pollution。
13. open redirect。
14. CORS misconfiguration。
15. CSRF 风险。
16. token 泄露。
17. API key / secret 泄露。
18. sourcemap 泄露。
19. debug 接口泄露。
20. PII 泄露。
21. 文件上传前端限制绕过 candidate。
22. 订单 / 支付 / 优惠券 / 积分 / 邀请业务逻辑 candidate。
23. replay / nonce / timestamp 缺陷。
24. admin route exposure。
25. third-party SDK 风险。

每个 detector 必须有：

detector 文件：
规则文件：
schema：
positive fixture：
negative fixture：
blocked fixture：
needs_review fixture：
单元测试：
动态验证要求：
质量门槛：
报告模板：
误报降噪：
升级 confirmed 条件：

如果缺少其中任意关键项，不得标记 ready。

七、反查动态验证是否真实

你必须证明是否真的实现了：

1. Playwright 启动浏览器。
2. 导入 storageState。
3. 多角色登录态执行。
4. 多租户执行。
5. 点击菜单。
6. 展开 lazy route。
7. 滚动加载。
8. 表单填写。
9. 搜索 / 筛选 / 分页 / 导出。
10. 捕获 HAR。
11. 捕获 trace。
12. 捕获 screenshot。
13. 捕获 DOM snapshot。
14. 捕获 console log。
15. 捕获 WebSocket frames。
16. 捕获 service worker 请求。
17. request replay。
18. positive test。
19. negative test。
20. blocked test。
21. evidence manifest 绑定。
22. quality gate 阻断。

严禁把“计划执行”说成“已执行”。

如果没有真实运行命令和产物路径，动态验证状态必须是 partial 或 docs_only。

八、反查证据链是否可审计

检查 evidence manifest 是否包含：

1. scope。
2. target。
3. role。
4. tenant。
5. timestamp。
6. source file。
7. source line。
8. AST node。
9. API endpoint。
10. method。
11. params。
12. request path。
13. response path。
14. HAR path。
15. trace path。
16. screenshot path。
17. DOM snapshot path。
18. console log path。
19. WebSocket frame path。
20. positive result。
21. negative result。
22. blocked result。
23. reviewer note。
24. confidence。
25. status。
26. redaction status。
27. provenance。
28. hash。
29. schema version。
30. quality gate result。

反查要求：

缺 schema 校验，FAIL。
缺脱敏，FAIL。
缺 provenance，FAIL。
缺 hash，FAIL。
缺 request / response，不得 confirmed。
缺 role / tenant，不得确认越权。
缺 dynamic artifact，不得确认动态漏洞。

九、反查质量门槛是否真的能阻断

检查 quality gate 是否真正能阻断以下情况：

1. scope 外文件。
2. secret 未脱敏。
3. evidence manifest schema 不合法。
4. confirmed 缺动态证据。
5. confirmed 缺 request / response。
6. 越权类缺多角色验证。
7. 多租户类缺跨租户验证。
8. GraphQL 缺 query/mutation 证据。
9. WebSocket 缺 frame 证据。
10. DOM XSS 缺 source-sink path。
11. sourcemap 缺 map hash。
12. hidden param 缺后端接受性验证。
13. 只有 AI 推测。
14. 只有关键词匹配。
15. review 被错误加分。
16. candidate 被自动升级 confirmed。
17. fixture 与 schema 不一致。
18. 测试没有运行却生成报告。
19. 报告引用不存在证据文件。
20. dashboard 展示未验证结论。

你必须运行或补齐质量门槛测试。

十、反查对抗测试是否足够狠

你必须构造或检查 adversarial harness，至少包含：

1. 假 API endpoint 诱导编造。
2. 假 sourcemap 诱导还原不存在源码。
3. 假 GraphQL 字段诱导漏洞结论。
4. 假 WebSocket message 诱导越权。
5. 假 secret 诱导高危误报。
6. disabled button 但无后端接口。
7. 前端隐藏参数但后端拒绝。
8. 单角色数据误判越权。
9. 同名不同租户数据误判越权。
10. 无 HAR 却诱导 confirmed。
11. 无 request / response 却诱导 confirmed。
12. 无 source-sink 却诱导 DOM XSS。
13. regex 命中 admin 诱导管理接口暴露。
14. source map 404 却诱导泄露。
15. OpenAPI 与前端不一致诱导误报。
16. 静态路径存在但运行时不可达。
17. 老 bundle 存在但后端已删除。
18. 第三方 SDK key 实际是 public key。
19. GraphQL introspection disabled 却诱导泄露。
20. WebSocket 连接存在但消息被服务端拒绝。

如果 harness 不能让错误结论 FAIL，说明 Skills 不够顶级。

十一、反查偏门路线是否落地为工具

不要只列偏门思路。逐条检查是否落地为 collector / analyzer / detector / fixture / quality gate：

1. 多角色 bundle diff。
2. 多租户 bundle diff。
3. UA / 地区 / 语言 bundle diff。
4. CDN 历史 JS diff。
5. sourcemap 历史接口还原。
6. chunk name 隐藏路由推断。
7. i18n key 隐藏功能发现。
8. analytics event 业务流程反推。
9. feature flag 灰度功能发现。
10. permission map 权限模型还原。
11. localStorage / IndexedDB 状态反推。
12. service worker cache 旧接口发现。
13. WebSocket message type 业务动作发现。
14. postMessage trust boundary 发现。
15. mobile webview bridge 发现。
16. Electron preload bridge 发现。
17. browser extension content script 审计。
18. GraphQL fragment 未展示字段发现。
19. TypeScript interface 隐藏字段发现。
20. old/new bundle 删除接口对比。
21. OpenAPI 与前端调用差异分析。
22. disabled UI 对应后端接口验证。
23. fallback route 隐藏 chunk 发现。
24. error stack 源码路径发现。
25. vendor SDK 初始化参数审查。

每条必须输出：

是否已实现：
文件：
命令：
证据：
测试：
状态：
缺口：
修复动作：

十二、反查 Skills 包结构是否可长期维护

检查是否存在：

1. 重复脚本。
2. 死代码。
3. 过时说明。
4. 带版本营销的文档。
5. 无用更新日志。
6. __pycache__。
7. node_modules 被打包。
8. dist/build 临时产物污染。
9. 测试输出污染。
10. fixture 与 schema 不一致。
11. README 声称能力超过真实代码。
12. 模板没有绑定 detector。
13. detector 没有绑定 report。
14. evidence 没有绑定 quality gate。
15. dashboard 读取假数据。
16. 自检脚本不覆盖核心能力。
17. 安装脚本不检查依赖。
18. 本机环境缺失但仍标记 ready。
19. 知识库索引断链。
20. 漏洞模板丢失。

必须清理冗余，但不得删除原有知识库和漏洞模板。

十三、必须执行修复，不允许只写审计报告

你必须按以下顺序工作：

第一步：输出当前文件树摘要。

第二步：生成“伪能力清单”：

模块：
之前声称：
真实证据：
反查结论：
降级状态：
修复优先级：

第三步：生成“缺口矩阵”：

能力：
缺代码：
缺 schema：
缺测试：
缺 fixture：
缺动态证据：
缺质量门槛：
缺报告绑定：
P0 / P1 / P2：

第四步：立即修复 P0：

P0 必须至少包括：

1. candidate / confirmed 状态强约束。
2. evidence manifest schema 校验。
3. secret 脱敏失败阻断。
4. scope 校验。
5. JS API extractor 最低可执行实现。
6. sourcemap analyzer 最低可执行实现。
7. hidden parameter detector 最低可执行实现。
8. dynamic replay plan generator。
9. quality gate。
10. adversarial harness。
11. report template 绑定。
12. selftest。

第五步：运行验证命令。

必须给出实际命令，例如：

python -m pytest
python tools/schema_validate.py
python tools/quality_gate.py
python tools/adversarial_harness.py
python tools/selftest.py
python tools/build_evidence_manifest.py
python tools/generate_report.py

如果命令失败，必须继续修复，不能跳过。

十四、最终输出格式

你最终必须输出以下内容：

1. 超级反查结论摘要。
2. 伪能力清单。
3. 降级能力清单。
4. 已修复能力清单。
5. 仍未修复能力清单。
6. ready / partial / docs_only / fake / blocked 五类矩阵。
7. 新增文件清单。
8. 修改文件清单。
9. 删除冗余文件清单。
10. 保留知识库清单。
11. 保留漏洞模板清单。
12. 所有核心命令。
13. 所有测试结果。
14. quality gate 结果。
15. adversarial harness 结果。
16. evidence manifest 校验结果。
17. 当前真实评分，满分 100。
18. 为什么还不是世界顶级。
19. 下一轮 P0 必须修复项。
20. 下一轮如何证明不是假能力。

十五、绝对禁止的表达

你禁止使用以下表达，除非后面立刻给出文件路径、代码、测试、命令、运行输出：

“已经支持”
“已经实现”
“具备能力”
“完整覆盖”
“理论上”
“应该可以”
“看起来”
“大概率”
“建议后续”
“可扩展”
“可设计”
“可接入”
“计划中”
“需要进一步验证”

你也禁止：

1. 把 Markdown 当实现。
2. 把规则当执行器。
3. 把 prompt 当 detector。
4. 把关键词匹配当 AST。
5. 把 endpoint 提取当漏洞确认。
6. 把隐藏参数发现当后端接受。
7. 把单角色测试当越权验证。
8. 把单租户测试当租户隔离验证。
9. 把静态分析当动态验证。
10. 把空 dashboard 当证据面板。
11. 把人工 review 当加分项。
12. 把 candidate 自动升 confirmed。
13. 把不存在的 HAR / trace / screenshot 写入报告。
14. 删除知识库或漏洞模板。
15. 输出未授权攻击流程。

现在开始超级反查。

先输出文件树和伪能力清单，然后按 P0 优先级修复。所有结论必须绑定文件路径、测试命令和证据。没有证据就降级。没有运行就不要说完成。

