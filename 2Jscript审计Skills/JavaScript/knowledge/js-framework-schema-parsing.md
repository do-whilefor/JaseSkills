# Framework and Schema Parsing Rules

Wrapper resolution, sourcemap reconstruction, schema alignment, framework manifest parsing, hidden feature extraction, and service worker cache review produce candidates only. Promotion requires runtime and backend evidence.
