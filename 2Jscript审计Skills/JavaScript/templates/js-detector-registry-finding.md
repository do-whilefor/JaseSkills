# Detector Finding

rule_id:
status: candidate / needs_review / confirmed / false_positive
source file:
line:
ast node:
call chain:
api endpoint:
method:
params:
authn:
authz:
tenant boundary:
sink:
request evidence:
response evidence:
HAR path:
trace path:
screenshot path:
DOM snapshot path:
positive test:
negative test:
blocked test:
reviewer note:
promotion blockers:
redaction status:
provenance hash:

禁止：没有 evidence manifest、request/response、动态 replay 或多角色/多租户验证时写 confirmed。
