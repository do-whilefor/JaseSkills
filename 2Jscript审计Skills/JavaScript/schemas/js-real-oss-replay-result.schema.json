{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "js-real-oss-replay-result.schema.json",
  "type": "object",
  "required": ["schema_version", "status", "project", "commands", "outputs"],
  "properties": {
    "schema_version": {"const": "js-real-oss-replay-result/v1"},
    "status": {"enum": ["ready", "partial", "environment_blocked", "missing"]},
    "project": {"type":"object"},
    "commands": {"type":"array"},
    "outputs": {"type":"object"}
  },
  "additionalProperties": true
}
