1. 认证绕过。
2. 水平越权。
3. 垂直越权。
4. 多租户隔离绕过。
5. 后端接受前端未暴露参数导致的越权或逻辑绕过。
6. IDOR。
7. SSRF
8. 文件上传导致的任意文件写入、路径穿越、危险文件解析。
9. 任意文件读取，但只能读取测试目录中的 marker 文件。
10. 模板注入，但只做无害 marker 级验证。
11. 命令执行风险，但只做本地测试环境中的无害 marker 级验证，不执行破坏性命令。
12. SQL 注入，但只在测试数据库中使用只读、可回滚、无破坏验证。
13. 反序列化风险，但只使用无害 gadget 或 marker 级验证。
14. 业务逻辑漏洞，例如优惠、余额、审批、邀请、重置密码、邮箱绑定、OAuth 绑定、Webhook 签名、订单状态机、幂等绕过。
15. 供应链漏洞，包括依赖版本、危险依赖、postinstall、动态加载、插件机制、模板包、构建脚本。
16. 密钥泄露、默认密钥、弱 JWT secret、调试 token、测试账号、硬编码凭据。
17. 安全边界错误，例如只在前端鉴权、只隐藏按钮、不在后端校验、只校验当前页面但不校验 API。
18. 会话与 Token 安全：JWT、session、refresh token、remember-me、退出登录失效、token 绑定、token 轮换。
19. OAuth / OIDC / SAML / SSO：redirect_uri、state、nonce、audience、issuer、账号绑定、邮箱归属、回调逻辑。
20. 密码重置、邮箱验证、邀请链接、一次性 token：重放、未绑定用途、未绑定用户、有效期、状态变更后失效。
21. CSRF 高危操作：改邮箱、改密码、绑定第三方账号、创建管理员、修改 webhook、修改支付信息。
22. CORS 凭证跨域错误：Origin 反射、credentials 误开、子域信任过宽。
23. Host Header / X-Forwarded-* 信任错误：密码重置链接、OAuth 回调、租户识别、缓存 key、审计日志。
24. HTTP 方法覆盖与路由差异：HEAD、OPTIONS、PATCH、PUT、DELETE、X-HTTP-Method-Override、_method。
25. 多解析器差异：JSON、form-data、multipart、XML、YAML、重复参数、数组、null、空字符串、嵌套对象。
26. 路径归一化与路由混淆：尾斜杠、大小写、编码、双重编码、反斜杠、Unicode、fallback。
27. 请求走私 / 协议解析错配：仅限本地授权代理与后端组合验证，不对公网测试。
28. 缓存投毒与私有缓存泄露：userId、tenantId、role、Authorization、Cookie、Host 是否进入 cache key。
29. 队列、异步任务、定时任务权限缺失：worker 是否二次鉴权，任务参数是否可信。
30. WebSocket / SSE / Socket.IO 事件级鉴权缺失。
31. GraphQL resolver 绕过、字段级权限缺失、mutation 隐藏参数、跨租户嵌套读取。
32. gRPC / RPC / internal API / debug API / metrics / actuator 暴露。
33. XXE / XML / SVG / Office / SAML 解析风险：仅用本地 marker 和本地 canary 验证。
34. NoSQL / LDAP / ORM / 搜索语法注入。
35. 表达式注入：SpEL、OGNL、MVEL、Jinja、规则引擎、工作流表达式。
36. JavaScript 原型污染 / 对象污染。
37. Mass Assignment / Model Binding：role、isAdmin、ownerId、tenantId、status、balance、approved、permission。
38. 业务状态机绕过：订单、审批、退款、优惠、余额、邀请、幂等、重复提交。
39. 条件竞争 / TOCTOU：仅本地测试数据、低频验证，不做压力破坏。
40. 文件处理二阶段漏洞：预览、转码、缩略图、OCR、PDF、Office、EXIF、SVG、对象存储回调。
41. 压缩包与归档格式：Zip Slip、tar、软链接、硬链接、重复文件名、Unicode、路径覆盖。
42. 静态资源与构建产物泄露：source map、.env、备份、日志、覆盖率报告、前端隐藏接口。
43. CI/CD 与构建脚本风险：Actions、CI、Dockerfile、compose、Makefile、package scripts、postinstall。
44. Docker / 镜像 / 部署配置风险：镜像层 secret、root、危险 volume、Docker socket、调试端口。
45. 供应链可达性风险：dependency confusion、lockfile 漂移、动态 require、插件包、模板包。
46. 插件 / 主题 / 模板 / 扩展系统边界：沙箱、签名、权限隔离、动态加载。
47. 多租户非主链路隔离：日志、通知、导出、搜索、报表、缓存、队列、附件、审计记录。
48. 审计日志与错误处理泄露：token、cookie、Authorization、SQL、环境变量、堆栈、内部路径。
49. 安全中间件顺序错误：路由组、fallback、异常处理、静态资源、admin 路由是否遗漏鉴权。
50. AI / LLM 功能安全：prompt injection、工具调用越权、文件读取边界、知识库越权、跨用户上下文污染。




