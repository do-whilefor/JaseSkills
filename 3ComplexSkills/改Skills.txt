你现在不是普通代码助手，而是“世界顶级授权漏洞挖掘 Skills 架构师 / 开源项目安全研究负责人 / AI 幻觉追责官 / 证据链法庭审查官 / 工程落地验收官”。

我已经在本机搭建了一个开源项目，并且生成了一个用于“开源项目 / 我明确授权项目”的信息收集与漏洞挖掘 Skills。你的任务不是夸它、不是美化它、不是泛泛建议，而是对它进行彻底审查、攻击性反查、工程级修复，让它尽可能接近世界顶级漏洞挖掘 Skills。

重要边界：

1. 只允许用于本机开源项目、靶场、测试环境、我明确授权的项目。
2. 禁止引导攻击第三方真实目标。
3. 禁止生成破坏性利用、持久化、横向移动、数据窃取、规避追踪、真实未授权利用流程。
4. 所有漏洞验证必须优先采用非破坏性验证。
5. 严重漏洞不得仅凭静态代码线索确认，必须有可复现证据链。
6. candidate、needs_review、confirmed 必须严格分级，禁止把候选线索伪装成 confirmed 漏洞。
7. 不允许编造证据，不允许假装运行过脚本，不允许把不存在的文件、命令、结果写进报告。

你的总目标：

把这个 Skills 改造成一个面向开源和授权项目的顶级漏洞挖掘系统，覆盖：

- 项目整体信息收集
- 代码结构理解
- 技术栈识别
- 架构图谱生成
- 入口面发现
- API / 路由 / 参数发现
- JS / 前端资产发现
- 后端真实接受参数发现
- 权限边界分析
- 多角色 / 多租户矩阵分析
- 业务逻辑漏洞发现
- 严重漏洞候选发现
- 0day 研究型线索发现
- 动态非破坏性验证
- 证据链生成
- 报告映射
- 质量门禁
- AI 幻觉防控
- 偏门、冷门、剑走偏锋路线补全

你必须先完整读取当前项目目录和 Skills 目录，不允许只看 README。请按下面顺序执行。

一、先做 Skills 全量盘点

请输出当前 Skills 的真实结构，包括：

1. 顶层目录结构。
2. 每个核心脚本的作用。
3. 每个 Markdown 规则文件的作用。
4. 每个模板文件的作用。
5. 每个知识库文件的作用。
6. 每个测试 / fixture / schema 的作用。
7. 当前执行入口是什么。
8. 当前输出物是什么。
9. 当前哪些能力是真实可执行的。
10. 当前哪些能力只是文档宣称但没有实现。

要求：

- 必须给出具体文件路径。
- 必须指出哪些文件是核心文件，哪些是冗余文件。
- 必须指出哪些文件会影响漏洞结论。
- 必须指出哪些文件只影响文档展示。

二、攻击性审查这个 Skills 是否真的能挖严重漏洞

请不要默认它有用。你要站在追责角度审查：

1. 它是否真的理解项目架构？
2. 它是否真的能识别框架？
3. 它是否真的能发现入口面？
4. 它是否真的能抽取路由？
5. 它是否真的能发现隐藏 API？
6. 它是否真的能发现前端未暴露但后端接受的参数？
7. 它是否真的能发现权限中间件缺失？
8. 它是否真的能发现 IDOR / 越权？
9. 它是否真的能发现多租户隔离失败？
10. 它是否真的能发现 SSRF、文件读取、模板注入、反序列化、命令注入、SQL/NoSQL 注入、GraphQL 滥用、WebSocket 越权、OAuth/OIDC 配置错误、JWT 错误、CORS 错误、缓存投毒、请求走私、原型污染、供应链风险、CI/CD 泄露、云凭据泄露？
11. 它是否真的能发现业务逻辑漏洞？
12. 它是否真的能从代码差异、异常分支、兼容逻辑、feature flag、debug 入口、旧版本遗留逻辑中挖出高危线索？
13. 它是否真的能将候选漏洞转换为非破坏性验证计划？
14. 它是否真的能生成 evidence manifest？
15. 它是否真的能区分 candidate / needs_review / confirmed？
16. 它是否有能力防止 AI 编造漏洞？
17. 它是否有能力防止 AI 把未运行过的验证写成已验证？
18. 它是否有质量门禁阻止低证据高危结论进入最终报告？

每一个问题都必须回答：

- 当前是否支持。
- 证据文件路径。
- 失败原因。
- 安全影响。
- 修复位置。
- 修复方式。
- 验证命令。
- 预期输出。

三、把信息收集升级成顶级漏洞挖掘前置系统

请检查并补强信息收集模块，使它不仅收集表面信息，还要服务严重漏洞挖掘。

必须覆盖：

1. 项目元信息  
   - package / dependency / lockfile
   - Dockerfile / Compose / Kubernetes / Helm
   - CI/CD
   - env 示例文件
   - 配置文件
   - README / docs / changelog / migration
   - 测试文件
   - seed / fixture
   - demo / example
   - admin / internal / debug 目录

2. 入口面发现  
   - HTTP 路由
   - RPC
   - GraphQL schema / resolver
   - WebSocket handler
   - queue / job / worker
   - CLI command
   - cron task
   - webhook
   - upload / download
   - import / export
   - callback / redirect
   - OAuth / SSO / SAML / OIDC
   - admin panel
   - debug endpoint
   - health / metrics / actuator
   - internal API

3. 参数发现  
   - query
   - body
   - path
   - header
   - cookie
   - multipart
   - JSON nested field
   - GraphQL variable
   - WebSocket message field
   - hidden frontend parameter
   - backend accepts but frontend does not send 的参数
   - mass assignment 参数
   - role / tenant / owner / status / price / amount / permission / scope / redirect / url / file / path / template / command 等敏感参数

4. 关系图谱  
   必须形成：

   Route → Handler → Middleware → AuthN → AuthZ → Validator → Parameter → Model → Query → Sink → Response → Evidence

5. 安全上下文  
   - 角色
   - 租户
   - 组织
   - 项目
   - 用户
   - 资源归属
   - 权限策略
   - feature flag
   - 付费等级
   - 审批状态
   - 工作流状态
   - 数据生命周期

四、把代码分析升级为真实语义分析，而不是 regex 幻觉

请检查当前是否只是 grep / regex / 文本匹配。如果是，必须升级为多后端 AST / code graph 架构。

至少要求：

1. JavaScript / TypeScript  
   - Babel parser
   - TypeScript Compiler API
   - source map 还原
   - dynamic import / lazy chunk
   - wrapper resolver
   - axios / fetch / graphql client / websocket client 解析
   - route / API client / 参数 / token / header 抽取

2. Python  
   - ast / libcst
   - FastAPI / Flask / Django route extractor
   - decorator / dependency / middleware 解析

3. Java  
   - Spring Controller / Filter / Interceptor / Security 注解
   - RequestMapping / PathVariable / RequestBody / RequestParam
   - MyBatis / JPA / JDBC sink

4. PHP  
   - Laravel / Symfony route
   - controller / middleware / request validation
   - Eloquent / DB sink

5. Go  
   - net/http / gin / echo / fiber route
   - middleware / handler / binding / SQL sink

6. Ruby  
   - Rails route / controller / before_action / params / ActiveRecord

输出必须是结构化 JSON，不允许只输出自然语言总结。

五、补齐严重漏洞挖掘引擎

请把漏洞发现从“关键词匹配”升级为“source-sink-dataflow-auth-tenant-aware detector”。

至少要有以下严重漏洞 detector：

1. IDOR / BOLA
2. 水平越权
3. 垂直越权
4. 多租户隔离失败
5. mass assignment
6. SSRF
7. 任意文件读取
8. 任意文件写入
9. 文件上传绕过
10. 路径穿越
11. 命令注入
12. SQL 注入
13. NoSQL 注入
14. GraphQL introspection / resolver 越权 / 查询复杂度滥用
15. WebSocket 鉴权缺失 / 消息级越权
16. JWT 错误验证
17. OAuth / OIDC / SAML 配置错误
18. CORS 高危配置
19. 原型污染
20. 模板注入
21. 反序列化
22. 缓存投毒
23. 请求走私风险线索
24. open redirect 到认证链风险
25. debug / internal endpoint 暴露
26. admin bypass
27. 业务逻辑漏洞
28. 价格 / 金额 / 数量 / 状态篡改
29. 支付 / 订单 / 优惠券 / 积分逻辑错误
30. 邀请 / 分享 / 协作权限错误
31. 审批流绕过
32. 文件导入导出越权
33. webhook / callback 信任边界错误
34. 队列 / worker 信任输入错误
35. CI/CD secret 泄露
36. 供应链依赖风险
37. 旧版本兼容分支导致的权限绕过
38. feature flag / beta endpoint 暴露
39. 测试账号 / seed 数据 / demo key 泄露
40. migration / backup / dump 泄露

每一个 detector 必须具备：

- detector_id
- 漏洞类型
- source 规则
- sink 规则
- sanitizer / validator 规则
- authn 规则
- authz 规则
- tenant 规则
- candidate 证据要求
- needs_review 证据要求
- confirmed 证据要求
- 非破坏性验证方法
- 正样本 fixture
- 负样本 fixture
- blocked 样本
- human_review 样本
- 报告模板映射
- quality gate 权重

六、补齐动态验证闭环

严重漏洞不能只靠静态分析。请补齐动态验证系统。

必须支持：

1. Playwright 真实浏览器
2. HAR 导入
3. Burp HAR 导入
4. trace 导入
5. screenshot 证据
6. DOM 快照
7. console 日志
8. request / response 摘要
9. storageState 登录态
10. 多角色 replay
11. 多租户 replay
12. 正向测试
13. 负向测试
14. 阻断测试
15. 非破坏性后端接受性测试
16. GraphQL replay
17. WebSocket replay
18. API 参数变异
19. 仅验证访问控制，不读取或破坏敏感数据
20. evidence manifest 自动生成

任何严重漏洞从 candidate 升级到 confirmed，必须满足：

- 有静态证据。
- 有动态证据。
- 有角色 / 租户上下文。
- 有请求响应证据摘要。
- 有非破坏性验证说明。
- 有 blocked 或 negative 对照。
- 有可复现步骤。
- 有 evidence manifest。
- 有 quality gate 通过记录。

七、补齐偏门、冷门、剑走偏锋的研究路线

你必须把漏洞挖掘路线从常规扫描升级到深度研究。

请重点加入以下路线，但必须保持合法授权和非破坏性：

1. 差异路线  
   - git diff
   - 历史 commit
   - revert commit
   - hotfix commit
   - security fix commit
   - migration 变化
   - API 版本差异
   - 新旧权限逻辑差异

2. 影子入口路线  
   - deprecated endpoint
   - beta endpoint
   - feature flag endpoint
   - internal route
   - admin-only route
   - test-only route
   - debug route
   - hidden frontend chunk
   - source map route
   - mobile / electron / extension API

3. 参数幽灵路线  
   - 前端不发送但后端绑定
   - DTO 多余字段
   - ORM mass assignment
   - GraphQL hidden field
   - WebSocket hidden message type
   - header-only privilege
   - cookie flag
   - legacy status 字段
   - owner_id / tenant_id / role / scope / price / amount 等危险字段

4. 权限错位路线  
   - route 有 authn 没 authz
   - middleware 顺序错误
   - controller 有校验，service 没校验
   - list 接口校验，detail 接口缺失
   - update 校验，export 缺失
   - UI 隐藏按钮，但 API 未校验
   - admin UI 有限制，API 无限制
   - tenant filter 在部分查询缺失

5. 异步边界路线  
   - queue job
   - webhook
   - callback
   - cron
   - worker
   - import task
   - export task
   - email link
   - invite link
   - password reset
   - magic link
   - approval flow

6. 解析器差异路线  
   - URL parser 差异
   - path normalize 差异
   - JSON parser 差异
   - multipart parser 差异
   - proxy / backend header 差异
   - unicode / encoding / case sensitivity 差异
   - trailing slash / dot segment / double encoding

7. 供应链路线  
   - lockfile diff
   - abandoned dependency
   - postinstall script
   - package alias
   - dependency confusion 线索
   - transitive dependency risk
   - GitHub Actions secret exposure
   - Docker build secret
   - npm / pip / composer / go module / maven 配置风险

8. 架构裂缝路线  
   - monorepo 包边界
   - shared library 被错误复用
   - service-to-service trust
   - API gateway 与后端鉴权不一致
   - cache 与权限不一致
   - read model / write model 权限不一致
   - 数据同步导致的旧权限残留

八、建立 AI 幻觉防线

请加入强制规则：

1. 没有文件证据，不得输出漏洞。
2. 没有命令输出，不得声称已运行。
3. 没有动态证据，不得 confirmed 严重漏洞。
4. 没有 role / tenant 对照，不得 confirmed 越权类漏洞。
5. 没有 request / response 摘要，不得 confirmed API 漏洞。
6. 没有 negative / blocked 对照，不得 confirmed 访问控制绕过。
7. 只看到危险函数，不得直接判漏洞。
8. 只看到依赖版本，不得直接判可利用。
9. 只看到前端隐藏字段，不得直接判后端接受。
10. 不得用“可能存在严重漏洞”冒充结论。
11. 不得用“建议进一步验证”逃避，必须给验证命令和验证数据结构。
12. 所有结论必须绑定 evidence_id。

九、质量门禁

请实现或修复 quality gate。

质量门禁至少包括：

1. schema 校验。
2. evidence manifest 校验。
3. secret 脱敏校验。
4. 文件路径存在性校验。
5. 命令输出存在性校验。
6. detector 输出结构校验。
7. candidate / needs_review / confirmed 分级校验。
8. 严重漏洞动态证据校验。
9. 越权类多角色证据校验。
10. 多租户类 tenant evidence 校验。
11. 报告模板映射校验。
12. dashboard 输入一致性校验。
13. AI 幻觉诱导测试。
14. stale docs / legacy docs 检测。
15. fixture 正负样本覆盖率检测。

十、修复方式要求

你不能只说“建议添加”。你必须实际修改文件。

每一项修复必须输出：

1. 修改的文件路径。
2. 新增的文件路径。
3. 删除的冗余文件路径。
4. 变更原因。
5. 输入格式。
6. 输出格式。
7. 示例命令。
8. 测试命令。
9. 预期输出。
10. 失败时如何定位。
11. 是否影响原有知识库。
12. 是否影响原有漏洞模板。

要求：

- 必须保留原有知识库。
- 必须保留原有漏洞模板。
- 可以清理冗余文档、旧版本说明、无关更新日志、重复文件、__pycache__。
- 不得删除核心能力文件。
- 不得把真实能力降级成文档描述。
- 不得引入无法运行的伪代码充当实现。

十一、最终交付物

完成后必须输出：

1. 修复摘要。
2. 当前 Skills 能力矩阵。
3. 已实现能力。
4. 未实现能力。
5. 仍然不能声称的能力。
6. 距离世界顶级还差什么。
7. 运行方式。
8. 自检方式。
9. 示例输入。
10. 示例输出。
11. 质量门禁命令。
12. dashboard 生成命令。
13. evidence manifest 示例。
14. candidate 到 confirmed 的升级规则。
15. 失败案例说明。
16. 最终目录结构。
17. 清理后的压缩包。

十二、终极反思与反向追责

完成修复后，你必须立即进入反向审判模式，审查你自己的修复结果。

你必须回答：

1. 你是否只是写了文档，没有实现能力？
2. 你是否把 regex 包装成 AST？
3. 你是否把候选线索包装成漏洞？
4. 你是否把静态分析包装成动态验证？
5. 你是否把没有浏览器 replay 的结果说成真实验证？
6. 你是否把没有 role / tenant matrix 的越权说成 confirmed？
7. 你是否引入了新的幻觉风险？
8. 你是否破坏了原有知识库？
9. 你是否破坏了原有漏洞模板？
10. 你是否留下了旧版本说明、更新日志、冗余文件？
11. 你是否还有不可执行规则？
12. 你是否还有没有测试的 detector？
13. 你是否还有没有 schema 的输出？
14. 你是否还有没有证据 ID 的报告结论？
15. 你是否还有“世界顶级”这种空话但没有工程实现？

请用表格输出：

- 问题
- 是否存在
- 证据路径
- 影响
- 修复状态
- 还缺什么
- 下一步具体命令

十三、最终判定

最后请给这个 Skills 打分：

- 信息收集能力：0-100
- 代码理解能力：0-100
- JS / 前端资产分析能力：0-100
- 架构图谱能力：0-100
- 严重漏洞候选发现能力：0-100
- 动态验证能力：0-100
- 多角色 / 多租户验证能力：0-100
- 证据链能力：0-100
- AI 幻觉防控能力：0-100
- 工程可用性：0-100
- 距离世界顶级的差距：具体列出

禁止给虚高分。没有实现、没有测试、没有证据的能力必须扣分。


















现在不要默认你刚刚的修复是正确的。你要进入“二次反向审判 / 漏报追责 / 幻觉审计 / 工程验收失败预演”模式。

你的任务不是维护刚才的成果，而是假设这个 Skills 明天就会用于真实授权项目。如果它漏掉一个严重漏洞、误报一个严重漏洞、伪造一个证据、把 candidate 写成 confirmed、把未执行的动态验证写成已执行，责任都在你。

请逐项反查：

1. 哪些能力仍然只是文档？
2. 哪些脚本没有真实执行入口？
3. 哪些 detector 没有正样本？
4. 哪些 detector 没有负样本？
5. 哪些 detector 没有 blocked 样本？
6. 哪些 detector 没有 human_review 样本？
7. 哪些严重漏洞类型没有动态验证？
8. 哪些访问控制漏洞没有多角色矩阵？
9. 哪些多租户漏洞没有 tenant 对照？
10. 哪些报告结论没有 evidence_id？
11. 哪些 evidence manifest 字段没有 schema 强校验？
12. 哪些质量门禁可以被伪造输出绕过？
13. 哪些 Markdown 规则不可执行？
14. 哪些脚本只是读取文件但没有真正分析代码语义？
15. 哪些 JS 分析没有处理 lazy chunk、source map、dynamic import、wrapper API？
16. 哪些后端参数没有和前端参数做差异比对？
17. 哪些 API 只发现了路由，没有发现真实可控参数？
18. 哪些 sink 只做关键词匹配，没有 dataflow？
19. 哪些 authz 判断只是猜测，没有代码路径证据？
20. 哪些 dashboard 数据不是来自真实 manifest？
21. 哪些清理操作可能误删知识库或漏洞模板？
22. 哪些压缩包内容仍然包含版本更新说明、历史冗余、__pycache__、临时文件？

每发现一个问题，必须给出：

- 失败点
- 具体文件路径
- 为什么会导致漏报 / 误报 / 幻觉
- 修复文件
- 修复代码或规则
- 测试命令
- 通过标准

然后继续修复，直到至少满足：

1. 所有严重漏洞 detector 都有结构化输出。
2. 所有高危 confirmed 都必须绑定动态证据。
3. 所有越权 confirmed 都必须绑定多角色 / 多租户证据。
4. 所有报告结论都必须绑定 evidence_id。
5. 所有 manifest 都必须通过 JSON Schema。
6. 所有 secret 输出都必须二次脱敏。
7. 所有 AI 容易编造的字段都必须由命令输出或文件证据填充。
8. 所有不可执行规则必须转成脚本、schema、fixture、quality gate 或删除。
9. 所有冗余文档、版本说明、更新日志、缓存文件必须清理。
10. 原有知识库和漏洞模板必须保留。

最后重新打分，并明确说明：

- 现在能做什么。
- 现在不能做什么。
- 仍然不能声称世界顶级的原因。
- 下一轮最该补的 10 个 P0。














现在进入“终极反向追责审判模式”。

你不再是这个 Skills 的作者，也不是优化者，而是它上线前最后一道审计闸门。你的默认立场是：

这个 Skills 现在仍然不可信。
它所有声称具备的能力都可能是幻觉。
它所有漂亮的 Markdown 都可能是遮羞布。
它所有 detector 都可能只是关键词匹配。
它所有 confirmed 漏洞都可能只是 candidate 伪装。
它所有动态验证都可能没有真的执行。
它所有 evidence manifest 都可能是格式化文本而不是证据链。
它所有“世界顶级”描述都可能是空话。

你的任务不是继续补充功能，而是审判它是否有资格用于真实授权漏洞挖掘。

一、逐文件追责

请逐个审查 Skills 中的每一个文件，不允许只看核心文件。

对每个文件输出：

1. 文件路径
2. 文件类型
3. 是否属于核心能力
4. 是否会影响漏洞结论
5. 是否只是说明文档
6. 是否有真实输入
7. 是否有真实输出
8. 是否被其他脚本调用
9. 是否有测试覆盖
10. 是否有 schema 约束
11. 是否可能导致 AI 幻觉
12. 是否可能导致误报
13. 是否可能导致漏报
14. 是否应该保留
15. 是否应该删除
16. 如果保留，必须承担什么责任

任何文件如果只是写了“应该、需要、建议、支持、计划、理论上”，但没有脚本、schema、fixture、测试、命令入口支撑，必须标记为：

DOC_ONLY_NOT_CAPABILITY

二、逐能力追责

请把这个 Skills 声称具备的所有能力列出来，包括 README、规则文件、提示词、脚本注释、模板里暗示的能力。

对每个能力逐项审判：

1. 能力名称
2. 声称位置
3. 真实实现文件
4. 真实执行命令
5. 输入样例
6. 输出样例
7. 测试文件
8. schema 文件
9. fixture 文件
10. 失败样本
11. 是否能自动运行
12. 是否能稳定复现
13. 是否能防止 AI 编造
14. 是否能进入报告
15. 是否允许升级 confirmed
16. 最终判定

最终判定只能使用：

- REAL_CAPABILITY
- PARTIAL_CAPABILITY
- CANDIDATE_ONLY
- DOC_ONLY_NOT_CAPABILITY
- UNSAFE_OR_MISLEADING
- REMOVE_OR_REWRITE

禁止使用“基本支持”“看起来支持”“应该可行”“后续完善”这种模糊词。

三、严查漏洞 detector 是否是假货

请逐个审查所有漏洞 detector。

你必须证明每个 detector 不是下面这些伪能力：

1. 不是简单 grep。
2. 不是只匹配危险函数。
3. 不是只匹配关键词。
4. 不是只看依赖版本。
5. 不是只看路由名。
6. 不是只看参数名。
7. 不是只看注释。
8. 不是把 source 和 sink 分开列出来就假装 dataflow。
9. 不是把 authn 当 authz。
10. 不是把前端隐藏按钮当后端越权。
11. 不是把潜在风险当漏洞。
12. 不是把没有动态验证的严重漏洞写成 confirmed。

每个 detector 必须输出：

1. detector_id
2. 漏洞类型
3. 支持语言 / 框架
4. source 识别逻辑
5. sink 识别逻辑
6. dataflow 证明方式
7. authn 证明方式
8. authz 证明方式
9. tenant 证明方式
10. sanitizer / validator 识别方式
11. candidate 升级条件
12. needs_review 升级条件
13. confirmed 升级条件
14. 正样本路径
15. 负样本路径
16. blocked 样本路径
17. human_review 样本路径
18. 测试命令
19. 当前失败点
20. 是否允许进入最终报告

如果任何 detector 没有正样本、负样本、blocked 样本、human_review 样本，必须降级为：

CANDIDATE_ONLY

如果任何严重漏洞 detector 没有动态验证证据，必须禁止 confirmed。

四、严查信息收集是否只是表面枚举

你必须审查信息收集模块是否真的服务严重漏洞发现。

逐项检查它是否能发现：

1. 隐藏路由
2. 懒加载 JS chunk
3. source map
4. dynamic import
5. service worker
6. GraphQL schema
7. WebSocket message type
8. RPC 方法
9. webhook callback
10. cron / job / worker
11. CLI command
12. admin / internal / debug endpoint
13. deprecated endpoint
14. feature flag endpoint
15. test-only endpoint
16. mobile / electron / extension API
17. 后端接受但前端不发送的参数
18. mass assignment 参数
19. tenant_id / owner_id / role / scope / price / amount / status 等危险字段
20. CI/CD secret 线索
21. Docker / Compose / K8s / Helm 暴露面
22. migration / seed / fixture / backup / dump
23. old docs 与 current code 的差异
24. git history / hotfix / revert / security patch 线索

每一项都必须回答：

- 当前是否真实支持
- 对应脚本路径
- 输出字段
- 是否进入 evidence manifest
- 是否进入漏洞 detector
- 是否进入 dashboard
- 是否有测试
- 缺失会导致什么严重漏报

如果某项只在 Markdown 里出现，但没有代码实现，必须标记：

MENTIONED_BUT_NOT_IMPLEMENTED

五、严查架构图谱是否真的存在

你必须审查 Skills 是否真的能生成下面这条链路：

Route → Handler → Middleware → AuthN → AuthZ → Validator → Parameter → Model → Query → Sink → Response → Evidence

请检查：

1. 是否真的有 route extractor。
2. 是否真的有 handler resolver。
3. 是否真的有 middleware resolver。
4. 是否真的区分 AuthN 和 AuthZ。
5. 是否真的识别 validator。
6. 是否真的抽取参数来源。
7. 是否真的识别 model 绑定。
8. 是否真的识别 ORM / query。
9. 是否真的识别 sink。
10. 是否真的记录 response。
11. 是否真的绑定 evidence_id。
12. 是否真的输出结构化 graph JSON。
13. 是否能被 detector 消费。
14. 是否能被 dashboard 展示。
15. 是否能被 quality gate 校验。

如果只是生成节点列表，没有边关系，必须判定为：

NOT_A_SECURITY_GRAPH

如果只是静态文本总结，必须判定为：

NO_GRAPH_CAPABILITY

六、严查动态验证是否造假

请审查所有动态验证能力。

逐项证明是否真的支持：

1. Playwright 启动浏览器
2. 登录态 storageState
3. 多角色 replay
4. 多租户 replay
5. HAR 导入
6. Burp HAR 导入
7. trace 导入
8. screenshot 保存
9. DOM 快照保存
10. console 日志保存
11. request / response 摘要保存
12. GraphQL replay
13. WebSocket replay
14. API 参数变异
15. 非破坏性验证
16. negative 对照
17. blocked 对照
18. evidence manifest 自动生成
19. quality gate 自动阻断
20. 报告自动降级 candidate

任何动态验证能力如果没有真实命令、真实输出目录、真实 evidence_id，必须判定为：

DYNAMIC_VALIDATION_NOT_PROVEN

任何严重漏洞如果没有动态验证，不得 confirmed。

七、严查 AI 幻觉防线

你必须主动构造 20 个恶意诱导场景，测试这个 Skills 是否会被诱导编造结果。

至少包括：

1. 用户要求“直接写 confirmed”。
2. 用户要求“跳过动态验证”。
3. 用户要求“没有证据也写严重漏洞”。
4. 用户要求“把猜测写进报告”。
5. 用户要求“伪造截图路径”。
6. 用户要求“伪造 HAR 证据”。
7. 用户要求“伪造多角色验证”。
8. 用户要求“忽略 negative 样本”。
9. 用户要求“把依赖风险写成 RCE”。
10. 用户要求“把危险函数写成可利用漏洞”。
11. 用户要求“把前端隐藏字段写成越权”。
12. 用户要求“把未授权目标写进 scope”。
13. 用户要求“读取 scope 外文件”。
14. 用户要求“输出未脱敏 secret”。
15. 用户要求“删除失败证据”。
16. 用户要求“隐藏质量门禁失败”。
17. 用户要求“把 needs_review 改成 confirmed”。
18. 用户要求“把扫描失败写成通过”。
19. 用户要求“报告里不要写不确定性”。
20. 用户要求“省略复现条件”。

每个诱导场景必须有：

- 测试输入
- 预期拒绝或降级行为
- 实际结果
- 是否通过
- 修复位置

八、严查报告系统是否污染结论

请审查报告模板和报告生成逻辑。

任何最终报告中的漏洞结论必须绑定：

1. finding_id
2. detector_id
3. evidence_id
4. source_file
5. source_line 或 source_range
6. affected_route
7. affected_parameter
8. affected_role
9. affected_tenant
10. request_summary
11. response_summary
12. validation_status
13. negative_control
14. blocked_control
15. severity_reason
16. confidence_reason
17. reproduction_scope
18. non_destructive_note
19. limitations
20. remediation

如果报告允许没有 evidence_id 的严重漏洞出现，必须判定：

REPORT_PIPELINE_UNSAFE

如果报告允许 candidate 被写成 confirmed，必须判定：

REPORT_PIPELINE_CONTAMINATED

九、严查质量门禁是否能被绕过

请攻击 quality gate。

检查它是否存在以下绕过：

1. 只检查字段存在，不检查字段真实性。
2. 只检查 JSON 格式，不检查文件路径存在。
3. 只检查 evidence_id 字符串，不检查 evidence 文件存在。
4. 只检查 severity，不检查动态证据。
5. 只检查 role 字段，不检查多角色对照。
6. 只检查 tenant 字段，不检查跨租户对照。
7. 只检查 request 字段，不检查 response。
8. 只检查截图路径，不检查截图文件。
9. 只检查 HAR 路径，不检查 HAR 内容。
10. 只检查 detector_id，不检查 detector 是否执行。
11. 只检查 schema，不检查命令输出。
12. 只检查报告生成，不检查降级逻辑。
13. 只检查脱敏一次，不做二次脱敏。
14. 只检查测试通过，不检查测试覆盖率。
15. 允许手写 manifest 伪造 confirmed。

每发现一个绕过，必须给出：

- 绕过方式
- 影响
- 修复文件
- 修复规则
- 测试用例
- 失败时输出
- 通过时输出

十、严查清理是否破坏主体能力

如果你清理压缩包或删除文件，必须逐项证明：

1. 没有删除知识库。
2. 没有删除漏洞模板。
3. 没有删除 detector。
4. 没有删除 schema。
5. 没有删除 fixture。
6. 没有删除测试。
7. 没有删除执行入口。
8. 没有删除 dashboard 输入。
9. 没有删除 evidence manifest 样例。
10. 没有删除质量门禁规则。
11. 删除的只是缓存、临时文件、历史更新说明、重复文档、无关版本说明。
12. 删除后 hash 稳定。
13. 删除后测试仍通过。
14. 删除后目录结构更清晰。
15. 删除后能力没有降级。

十一、最终必须执行的本机验收命令

你必须给出并运行一组本机验收命令。不能只写“建议运行”。

至少包括：

1. 目录结构检查
2. 冗余文件检查
3. schema 校验
4. evidence manifest 校验
5. secret 脱敏测试
6. detector 单元测试
7. fixture 正负样本测试
8. blocked 样本测试
9. human_review 样本测试
10. candidate / needs_review / confirmed 分级测试
11. 动态验证导入测试
12. Playwright 环境自检
13. HAR 导入测试
14. GraphQL replay 测试
15. WebSocket replay 测试
16. dashboard 生成测试
17. report 生成测试
18. AI 幻觉诱导测试
19. 压缩包内容检查
20. 最终 hash manifest 生成

每个命令必须输出：

- 命令
- 作用
- 成功标准
- 失败标准
- 当前结果
- 如果失败，必须继续修复

十二、最终审判表

最后输出一张不可美化的审判表：

| 审判项 | 当前结论 | 证据路径 | 是否可执行 | 是否有测试 | 是否可进入报告 | 是否允许 confirmed | 失败风险 | 修复状态 |
|---|---|---|---|---|---|---|---|---|

审判项必须包括：

1. 信息收集
2. 代码语义分析
3. JS 资产分析
4. 后端参数发现
5. 架构图谱
6. IDOR / BOLA
7. 垂直越权
8. 多租户隔离
9. SSRF
10. 文件读取
11. 文件上传
12. 注入类
13. GraphQL
14. WebSocket
15. OAuth / OIDC / SAML
16. JWT
17. CORS
18. 业务逻辑
19. 供应链
20. CI/CD
21. 动态验证
22. 多角色矩阵
23. 证据链
24. 质量门禁
25. 报告生成
26. AI 幻觉防控
27. 压缩包清理
28. 知识库保留
29. 漏洞模板保留
30. 工程可用性

十三、强制降级规则

出现以下任何情况，必须自动降级：

1. 没有动态证据的严重漏洞：confirmed → candidate。
2. 没有多角色对照的越权：confirmed → needs_review。
3. 没有 tenant 对照的多租户漏洞：confirmed → needs_review。
4. 没有 request / response 摘要的 API 漏洞：confirmed → candidate。
5. 没有 source-sink 链路的注入类漏洞：confirmed → candidate。
6. 没有后端接受性验证的隐藏参数：confirmed → candidate。
7. 没有 schema 通过的 manifest：整份报告 FAIL。
8. 发现未脱敏 secret：整份报告 FAIL。
9. 发现证据路径不存在：对应 finding FAIL。
10. 发现伪造命令输出：整套 Skills FAIL。

十四、最后打分，但禁止虚高

请重新打分：

- 信息收集能力：0-100
- 代码语义理解：0-100
- JS / 前端深度分析：0-100
- 后端参数发现：0-100
- 架构图谱：0-100
- 严重漏洞候选发现：0-100
- 严重漏洞确认能力：0-100
- 动态验证：0-100
- 多角色 / 多租户验证：0-100
- 证据链：0-100
- 质量门禁：0-100
- AI 幻觉防控：0-100
- 工程可维护性：0-100
- 压缩包整洁度：0-100
- 距离世界顶级差距：必须列出 10 个最大短板

扣分规则：

1. 只有文档，没有实现：最高 30 分。
2. 有脚本，但没有测试：最高 50 分。
3. 有测试，但没有真实样本：最高 60 分。
4. 有静态分析，但没有动态验证：严重漏洞确认能力最高 50 分。
5. 有动态验证，但没有多角色 / 多租户：越权类最高 60 分。
6. 有报告，但没有 evidence_id：证据链最高 40 分。
7. 有 detector，但没有 source-sink-dataflow：严重漏洞发现最高 55 分。
8. 有 dashboard，但不读取真实 manifest：dashboard 相关能力最高 40 分。
9. 有质量门禁，但可被伪造 manifest 绕过：质量门禁最高 50 分。
10. 存在 AI 编造 confirmed 的路径：整套系统不得超过 70 分。

十五、最后一句话必须明确

最后必须用一句话明确回答：

这个 Skills 现在到底是：

1. 可以用于真实授权项目的漏洞挖掘生产工具；
2. 只能作为辅助候选发现工具；
3. 只是信息收集和审计框架雏形；
4. 主要还是文档型规则包；
5. 不建议使用。

必须选择一个，不允许模糊。


