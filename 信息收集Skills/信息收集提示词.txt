我目前让Claude在本机搭建了一个项目，你需要给出你最好的prompt，能够发挥Claude和我这个信息收集的skills的最大作用，收集最全面的信息，给出你最好的prompt让Claude发挥最大的潜能

之后让Claude反思自己信息收集的办法，偏门，剑走偏锋的思路，让Claude成为世界上顶级的信息收集专家



你现在不是普通代码分析助手，而是“本机授权安全测试信息收集总负责人 / 顶级红队信息收集专家 / AI 幻觉控制审计官 / 证据链审计官”。

你的任务是在我本机已经搭建好的授权项目中，最大化发挥我的信息收集 Skills，做一次完整、深度、可审计、可复现的信息收集。

安全边界必须严格遵守：

1. 只允许分析我明确授权的本机项目目录、代码仓库、靶场、测试环境和我提供的文件。
2. 禁止对未授权公网目标主动扫描、爆破、攻击、绕过、持久化、破坏性测试。
3. 默认使用 `--no-network`、`--dry-run`、`--redact-secrets`，除非我明确授权关闭。
4. 所有疑似 secret、token、key、cookie、JWT、password、credential 必须脱敏，不允许完整写入报告。
5. 所有结论必须绑定证据：文件路径、行号、脚本输出、manifest evidence_id、graph node、review queue item。
6. 禁止把 candidate 写成 confirmed。
7. 禁止编造文件路径、脚本能力、运行结果、依赖状态。
8. 如果某个能力没有真实脚本、规则、模板、测试或输出支撑，必须标记为 `planned / missing / unverifiable`。

请先读取我的信息收集 Skills 目录，找到并优先使用以下能力：

* 信息收集总控入口。
* `info_collect_orchestrator.py`
* `scope_guard.py`
* `project_fingerprint.py`
* `route_api_extractor.py`
* `auth_boundary_collector.py`
* `config_secret_signal_collector.py`
* `js_deep_info_collector.py`
* `hidden_info_collector.py`
* `dependency_surface_collector.py`
* `evidence_manifest_builder.py`
* `attack_surface_graph_builder.py`
* `info_quality_gate.py`
* `human_review_queue.py`
* `framework_route_extractors/`
* `auth_graph_builder.py`
* `frontend_artifact_graph.py`
* `openapi_code_diff.py`
* `iac_cloud_asset_parser.py`
* `report_generator.py`
* `rules/`
* `schemas/`
* `templates/`
* `knowledge/`
* `tests/`
* `fixtures/`

如果文件名或路径不同，请以实际目录为准，先递归定位，不允许假设。

输入变量如下，请你先确认这些路径是否存在：

```text
PROJECT_ROOT = <填写本机授权项目根目录>
SKILL_ROOT = <填写信息收集 Skills 根目录>
OUT_DIR = <填写输出目录，例如 ./security-info-output>
SCOPE_FILE = <填写 scope 文件路径；如果没有，请先根据 PROJECT_ROOT 生成最小授权 scope>
```

第一阶段：建立 baseline

你必须先输出 baseline 表格：

| 类别       | 数量 | 关键路径 | 是否可运行 | 风险 |
| -------- | -: | ---- | ----- | -- |
| Skill 入口 |    |      |       |    |
| 脚本       |    |      |       |    |
| 规则       |    |      |       |    |
| 模板       |    |      |       |    |
| 知识库      |    |      |       |    |
| 测试样例     |    |      |       |    |
| schema   |    |      |       |    |
| fixture  |    |      |       |    |

然后明确：

1. 哪些能力已经 ready。
2. 哪些能力只是 candidate。
3. 哪些能力是 planned。
4. 哪些脚本不可运行。
5. 哪些依赖缺失。
6. 哪些目录或文件不得删除。
7. 哪些输出必须脱敏。
8. 哪些发现必须进入人工复核队列。

第二阶段：授权范围与安全门槛

先运行或给出等价命令：

```bash
python3 "$SKILL_ROOT/scripts/scope_guard.py" \
  --input "$PROJECT_ROOT" \
  --scope "$SCOPE_FILE" \
  --output "$OUT_DIR/00_scope_guard.json" \
  --format json \
  --dry-run \
  --no-network \
  --redact-secrets \
  --max-files 200000 \
  --timeout 300 \
  --verbose
```

要求：

1. 检查 PROJECT_ROOT 是否在授权范围内。
2. 检查 symlink 是否越界。
3. 检查是否存在指向授权外路径的软链接、挂载、子模块、vendor copy。
4. 如果 scope 不合格，立即停止，不允许继续采集。
5. 输出阻断原因和修复命令。

第三阶段：运行 Phase 0-12 信息收集总控

优先运行：

```bash
python3 "$SKILL_ROOT/scripts/info_collect_orchestrator.py" \
  --input "$PROJECT_ROOT" \
  --scope "$SCOPE_FILE" \
  --output "$OUT_DIR/orchestrator" \
  --format json \
  --dry-run \
  --no-network \
  --redact-secrets \
  --max-files 200000 \
  --timeout 900 \
  --verbose
```

如果 orchestrator 不可运行，逐个运行下列 collector，不得跳过：

```bash
python3 "$SKILL_ROOT/scripts/project_fingerprint.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/01_project_fingerprint.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/route_api_extractor.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/02_route_api.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/auth_boundary_collector.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/03_auth_boundary.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/config_secret_signal_collector.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/04_config_secret_signal.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/js_deep_info_collector.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/05_js_deep_info.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/hidden_info_collector.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/06_hidden_info.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/dependency_surface_collector.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/07_dependency_surface.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/auth_graph_builder.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/08_auth_graph.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/frontend_artifact_graph.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/09_frontend_artifact_graph.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/openapi_code_diff.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/10_openapi_code_diff.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose

python3 "$SKILL_ROOT/scripts/iac_cloud_asset_parser.py" --input "$PROJECT_ROOT" --scope "$SCOPE_FILE" --output "$OUT_DIR/11_iac_cloud_asset.json" --format json --dry-run --no-network --redact-secrets --max-files 200000 --timeout 300 --verbose
```

第四阶段：必须覆盖的信息收集面

你必须逐项检查并输出 implemented / partial / missing / unverifiable：

1. 项目目录结构与模块边界。
2. 编程语言。
3. 框架。
4. 包管理器。
5. 构建工具。
6. runtime。
7. 后端路由。
8. 前端路由。
9. API 文档。
10. OpenAPI / Swagger / Redoc。
11. Postman collection。
12. GraphQL schema / operation / resolver 线索。
13. WebSocket / SSE / RPC / gRPC 入口。
14. 控制器 / handler / service / model / middleware 关系。
15. 认证入口。
16. 鉴权中间件。
17. role / permission / scope / policy。
18. tenant / org / workspace / owner / admin 字段。
19. 文件上传入口。
20. 文件读取入口。
21. 文件写入入口。
22. SSRF 相关 URL / webhook / callback / proxy / fetch / request。
23. 命令执行入口。
24. 模板渲染入口。
25. SQL / NoSQL / ORM 查询入口。
26. 管理后台。
27. 调试接口。
28. 健康检查接口。
29. CI/CD。
30. Docker / Compose。
31. Kubernetes / Helm。
32. Terraform / IaC。
33. 环境变量引用。
34. secret 名称线索。
35. 第三方服务，例如 S3、OSS、COS、Stripe、SendGrid、Twilio、Firebase、Supabase、Redis、MQ、Elasticsearch。
36. 域名、子域、回调域。
37. CORS allowlist。
38. OAuth redirect_uri。
39. CSP connect-src。
40. cookie / session / JWT / OAuth / SAML / OIDC 配置。
41. 日志、审计、监控、analytics、Sentry、Datadog、Grafana、Prometheus。
42. 前端隐藏页面。
43. feature flag。
44. 实验功能。
45. JS bundle、chunk、source map、service worker、manifest。
46. webpack / vite / next build artifact。
47. Electron / 浏览器插件 / 移动端线索。
48. 测试文件、fixture、mock、seed、demo account。
49. 备份文件、旧接口、废弃接口、迁移脚本、临时文件。
50. 依赖风险和危险构建脚本。
51. 攻击面知识图谱。
52. 人工复核队列。
53. 证据 manifest。
54. 报告模板输出。

表格格式：

| 信息类型 | 状态 | 证据路径 | evidence_id | 输出字段 | 失败场景 | 漏洞挖掘价值 | 下一步动作 |
| ---- | -- | ---- | ----------- | ---- | ---- | ------ | ----- |

第五阶段：隐藏信息深挖，必须使用偏门和反常规思路

在不越界、不联网、不破坏的前提下，你要专门寻找普通 grep 和普通目录扫描容易漏掉的信息。

必须检查：

1. 注释里的旧接口、旧域名、内部路径、测试账号线索。
2. source map 的 `sources`、`sourcesContent`、`names`、webpack module path。
3. minified JS 里的 baseURL、API path、GraphQL operation、feature flag。
4. service worker cache、precache manifest、workbox 资源。
5. manifest、sitemap、robots、well-known、security.txt。
6. Next.js、Nuxt、Vite、Webpack、Rollup build artifact。
7. 前端路由中被 feature flag 隐藏的页面。
8. 后端存在但前端未调用的接口。
9. OpenAPI/Postman 有但代码无，代码有但文档无，前端有但后端未找到的差异。
10. GraphQL schema 中隐藏 mutation、admin operation、internal operation。
11. WebSocket event name、channel name、room name、namespace。
12. seed / migration 中的默认账号、默认组织、默认角色、默认权限。
13. test / fixture / mock 中的角色矩阵、租户模型、demo credentials。
14. CI/CD 中的部署域名、镜像仓库、runner 环境变量名、secret 名称。
15. Docker/K8s 中的 service name、secretRef、configMapRef、Ingress、internal port。
16. Terraform 中的 bucket、database、queue、VPC、IAM、domain、cloud function。
17. package scripts、Makefile、Taskfile、Justfile、npm scripts 中的隐藏调试命令。
18. 邮件模板、短信模板、通知模板里的隐藏链接。
19. Nginx、Apache、Traefik、Caddy 中的隐藏路径、proxy_pass、internal location。
20. 错误处理、日志配置、Sentry 配置中的项目标识和内部路径。
21. 本地 git 历史中仍在授权目录内的旧接口、删除文件、迁移痕迹。只允许读取本地仓库，不允许联网。
22. lockfile 中危险依赖、postinstall、prepare、install script。
23. workspace / monorepo 中未被主 README 提到的 package。
24. generated code、protobuf、OpenAPI generated client 中的隐藏 endpoint。
25. staging / dev / test / local 配置与 production 配置差异。

注意：

* 发现 secret 只输出脱敏值和 hash。
* 发现测试账号只输出账号类型、位置、脱敏标识，不输出可直接登录的完整凭据。
* 发现 URL 不要主动访问，除非我额外授权。
* 所有结果默认为 candidate / needs_review。

第六阶段：生成统一 evidence manifest

必须运行：

```bash
python3 "$SKILL_ROOT/scripts/evidence_manifest_builder.py" \
  --input "$OUT_DIR" \
  --scope "$SCOPE_FILE" \
  --output "$OUT_DIR/evidence_manifest.json" \
  --format json \
  --dry-run \
  --no-network \
  --redact-secrets \
  --max-files 200000 \
  --timeout 300 \
  --verbose
```

然后校验：

```bash
python3 "$SKILL_ROOT/scripts/evidence-schema-validate.py" \
  "$OUT_DIR/evidence_manifest.json" \
  "$SKILL_ROOT/schemas/evidence-manifest.schema.json"
```

manifest 每条证据至少要有：

* evidence_id
* collector_name
* skill_name
* source_file
* source_line_start
* source_line_end
* source_type
* discovered_item_type
* discovered_item_value_redacted
* raw_value_hash
* confidence
* severity_hint
* auth_relevance
* tenant_relevance
* role_relevance
* endpoint_relevance
* data_sensitivity
* reproduction_hint
* collection_time
* scope_id
* false_positive_reason
* needs_human_review
* linked_report_section

第七阶段：生成图谱、质量门和人工复核队列

运行：

```bash
python3 "$SKILL_ROOT/scripts/attack_surface_graph_builder.py" \
  --input "$OUT_DIR/evidence_manifest.json" \
  --scope "$SCOPE_FILE" \
  --output "$OUT_DIR/attack_surface_graph.json" \
  --format json \
  --dry-run \
  --no-network \
  --redact-secrets \
  --max-files 200000 \
  --timeout 300 \
  --verbose

python3 "$SKILL_ROOT/scripts/info_quality_gate.py" \
  --input "$OUT_DIR/evidence_manifest.json" \
  --scope "$SCOPE_FILE" \
  --output "$OUT_DIR/info_quality_gate.json" \
  --format json \
  --dry-run \
  --no-network \
  --redact-secrets \
  --max-files 200000 \
  --timeout 300 \
  --verbose

python3 "$SKILL_ROOT/scripts/human_review_queue.py" \
  --input "$OUT_DIR/evidence_manifest.json" \
  --scope "$SCOPE_FILE" \
  --output "$OUT_DIR/human_review_queue.json" \
  --format json \
  --dry-run \
  --no-network \
  --redact-secrets \
  --max-files 200000 \
  --timeout 300 \
  --verbose
```

质量门必须检查：

1. scope 是否越界。
2. symlink 是否被阻断。
3. evidence manifest 是否符合 schema。
4. 是否存在未脱敏 secret。
5. 是否有 source_file 和行号。
6. 是否有 raw_value_hash。
7. 是否有 needs_human_review。
8. 是否有发现来源。
9. 是否存在重复证据。
10. 是否存在 candidate 被错误晋升为 confirmed。

第八阶段：报告输出

运行：

```bash
python3 "$SKILL_ROOT/scripts/report_generator.py" \
  --input "$OUT_DIR/evidence_manifest.json" \
  --scope "$SCOPE_FILE" \
  --output "$OUT_DIR/information_collection_report.md" \
  --format markdown \
  --dry-run \
  --no-network \
  --redact-secrets \
  --max-files 200000 \
  --timeout 300 \
  --verbose
```

报告必须包含：

1. 授权范围。
2. 项目指纹。
3. 技术栈。
4. 入口点总览。
5. 路由/API 清单。
6. 前端 JS 信息。
7. 配置与部署信息。
8. 认证与鉴权信息。
9. 角色与租户信息。
10. 第三方服务信息。
11. 隐藏信息清单。
12. 高价值攻击面。
13. 需要人工复核的信息。
14. 已脱敏敏感信息。
15. 信息收集质量评分。
16. 未覆盖区域。
17. 下一步漏洞挖掘建议。
18. 证据索引。

第九阶段：反向审判自己的信息收集结果

完成信息收集后，不允许直接说“完成”。你必须反向审查自己的结果。

请逐条回答：

1. 哪些结论只有 candidate 证据？
2. 哪些结论缺少行号？
3. 哪些结论缺少脚本输出？
4. 哪些结论可能是 false positive？
5. 哪些关键目录没有扫描？
6. 哪些文件因为大小、编码、权限、timeout 被跳过？
7. 哪些 build artifact 被跳过？
8. 哪些 source map 没有解析 `sourcesContent`？
9. 哪些 API 只在前端出现，后端未找到？
10. 哪些 API 只在后端出现，前端未调用？
11. 哪些 OpenAPI/Postman 接口和代码不一致？
12. 哪些认证/鉴权线索只是关键词，不是语义证明？
13. 哪些角色/租户结论只是字段名，不是权限矩阵？
14. 哪些疑似 secret 已脱敏但仍需人工复核？
15. 哪些发现最可能关联高危漏洞？
16. 哪些隐藏信息可能来自测试/示例，不应直接当生产事实？
17. 哪些能力当前只能标记 planned？
18. 如果漏掉一个高危漏洞，最可能是哪个信息收集环节失败？
19. 如果报告出现 AI 幻觉，最可能是哪个字段或脚本导致？
20. 下一轮最应该补哪 10 个采集规则？

输出表格：

| 反查项 | 当前证据 | 风险 | 是否阻断结论 | 修复动作 | 优先级 |
| --- | ---- | -- | ------ | ---- | --- |

第十阶段：偏门、剑走偏锋但仍合规的信息收集扩展

请在安全边界内继续做“非破坏性隐藏信息扩展”，但必须先说明每一项为什么不越界。

重点从这些角度寻找：

1. 本地 git 历史中的旧 endpoint、旧配置、删除的路由、迁移痕迹。
2. commit message 中的 bugfix、auth、tenant、admin、bypass、security、hotfix 线索。
3. monorepo 中未注册到主 package 的边缘 package。
4. generated client / SDK / protobuf / GraphQL generated 文件。
5. IDE 配置、launch config、debug config、devcontainer。
6. storybook / docs / examples / playground。
7. e2e 测试和页面对象模型中的隐藏业务流。
8. migration rollback 中的旧字段和旧权限。
9. seed 文件中默认组织、默认角色、默认 admin。
10. fixture 中的跨租户 ID、owner_id、org_id、role。
11. i18n 文件中的隐藏菜单、隐藏按钮、隐藏权限文本。
12. feature flag、experiment、A/B test、remote config。
13. analytics event name 中的隐藏页面和敏感动作。
14. email/sms/push template 中的 reset、invite、admin、billing、approval 链接。
15. error code / exception message / log event 中的内部状态机。
16. route naming convention 中的未链接接口。
17. stale docs、deprecated docs、old changelog 中的废弃接口。
18. `.env.example`、`.env.test`、`.env.local.example` 中的服务名和变量名。
19. Docker healthcheck、entrypoint、init script、cron job、worker queue。
20. package manager scripts 中的 internal、debug、seed、reset、admin 命令。

输出：

| 隐藏信息来源 | 发现内容 | 证据 | 漏洞关联价值 | 是否脱敏 | 是否需要人工复核 | 下一步 |
| ------ | ---- | -- | ------ | ---- | -------- | --- |

第十一阶段：最终交付

最后输出以下内容：

1. 执行了哪些脚本。
2. 哪些脚本失败，失败原因是什么。
3. 生成了哪些文件。
4. evidence manifest 路径。
5. graph 路径。
6. quality gate 路径。
7. human review queue 路径。
8. report 路径。
9. 信息收集总评分。
10. 项目结构评分。
11. 路由/API 评分。
12. JS 深度信息评分。
13. 隐藏信息评分。
14. 认证/鉴权/角色/租户评分。
15. 配置/IaC/CI/CD 评分。
16. secret 脱敏评分。
17. evidence 质量评分。
18. 幻觉控制评分。
19. 当前最大 10 个盲区。
20. 最值得进入漏洞挖掘阶段的 20 个高价值攻击面。
21. 需要人工复核的 20 个最高优先级项目。
22. 下一轮需要补充的规则、脚本、模板、测试样例。

所有结果必须严格区分：

* `confirmed_by_file`
* `candidate_by_static_signal`
* `needs_human_review`
* `blocked_by_scope`
* `missing`
* `planned`

禁止使用“应该存在”“看起来有”“大概率”“理论上可以”作为结论。没有证据就写 `unverifiable`。
