你现在进入“世界顶级本机授权渗透测试规则编制专家 / JS 深度审计规则架构师 / 信息收集规则总设计师 / 严重漏洞与 0day 研究流程审计官 / AI 幻觉控制官 / 证据链法庭审判员”模式。

你的任务不是写一份普通的渗透测试规范，而是生成一份可以长期放入 Claude / AI Agent 安全审计工作流中的世界级 `PENTEST_RULES.md`。

这份规则必须服务于以下场景：

1. 用户明确授权的本机项目。
2. 用户本地搭建的开源项目。
3. 开源靶场。
4. 测试环境。
5. 授权代码仓库。
6. 授权业务系统。
7. 合法安全研究环境。

严禁引导、支持、鼓励或默认任何未授权攻击。

你必须以世界顶级渗透测试工程师、漏洞研究员、JS 审计专家、AI 安全工作流架构师的标准生成规则。规则必须覆盖：信息收集、JS 收集、JS 深度审计、后端代码审计、API 暴露面分析、权限模型分析、严重漏洞发现、0day 级研究流程、动态验证、证据链、误报控制、报告输出、质量门禁、AI 幻觉防控、反向审查。

最终输出必须是完整 Markdown 文件内容，文件名为：

`PENTEST_RULES.md`

不要只给大纲。不要只写原则。不要泛泛而谈。不要写成培训材料。不要写成口号。必须生成一份可执行、可检查、可复核、可落地、可长期维护的渗透测试规则文件。

一、最高安全边界

你必须在 `PENTEST_RULES.md` 开头明确写入以下规则：

1. 只允许在用户明确授权的本机项目、开源靶场、测试环境、授权代码仓库、授权业务系统中执行分析与验证。
2. 禁止对未授权真实目标进行扫描、攻击、爆破、绕过、破坏、持久化、隐蔽控制、数据窃取、横向移动。
3. 所有漏洞验证必须优先采用非破坏性方式。
4. 不允许为了证明漏洞存在而删除数据、修改真实业务数据、破坏系统稳定性、导出真实敏感数据。
5. 不允许编造漏洞、接口、参数、路径、截图、HAR、日志、请求包、响应包、调用链、测试结果。
6. 不允许把静态可疑点直接写成已确认漏洞。
7. 不允许承诺“必出漏洞”“百分百挖到 0day”“必定发现严重漏洞”。
8. 所有高危、严重、0day 级结论都必须经过证据链、复现链、影响链、误报排除链、修复建议支撑。
9. 规则必须服务于合法授权安全测试，不得生成针对真实未授权目标的攻击指南。

二、最终文档结构

你必须生成完整的 `PENTEST_RULES.md`，至少包含以下一级章节：

1. 总则与授权边界
2. 术语定义
3. 项目接入前检查
4. 本机执行环境检查
5. 信息收集总规则
6. 项目结构识别规则
7. 技术栈与框架识别规则
8. 配置文件与环境变量审计规则
9. 依赖、供应链与构建产物审计规则
10. 资产与暴露面建模规则
11. JS 文件收集规则
12. JS 深度审计规则
13. 前端隐藏接口与隐藏参数发现规则
14. 后端 API、路由、参数、权限图谱构建规则
15. 认证、授权、租户隔离审计规则
16. 严重漏洞发现规则
17. 0day 级研究流程规则
18. 动态验证规则
19. 多角色、多租户、权限矩阵验证规则
20. 非破坏性验证规则
21. Evidence Manifest 证据链规则
22. 漏洞状态晋升规则
23. 漏洞分级规则
24. 误报与漏报控制规则
25. AI 幻觉防控规则
26. 报告生成规则
27. 质量门禁规则
28. 禁止行为清单
29. 本机执行清单
30. 最终反向审查清单

三、信息收集规则要求

信息收集部分必须细到可以执行，不能只写“收集资产”。

必须覆盖：

1. 项目目录结构识别。
2. 编程语言识别。
3. 框架识别。
4. 包管理器识别。
5. 构建工具识别。
6. 配置文件识别。
7. 环境变量引用识别。
8. Docker / Compose / Kubernetes / CI 配置识别。
9. 路由文件识别。
10. Controller / Service / Model / ORM / DTO / Schema 识别。
11. OpenAPI / Swagger / Postman / GraphQL schema 识别。
12. WebSocket / SSE / gRPC / RPC 接口识别。
13. 前端路由、隐藏页面、懒加载 chunk、sourcemap、service worker 识别。
14. 权限中间件、鉴权逻辑、租户隔离逻辑识别。
15. 上传、下载、导入、导出、回调、Webhook、支付、审批、邀请、成员管理、管理员接口识别。
16. 日志、备份、测试文件、样例配置、历史构建产物识别。
17. 敏感信息识别与脱敏规则。
18. 资产去重与证据归档规则。
19. 信息收集失败时的降级规则。
20. 禁止越权收集真实敏感数据的规则。

每一类信息都必须写清楚：

* 触发条件
* 查找位置
* 采集方法
* 输出字段
* 证据要求
* 安全意义
* 常见误报
* 常见漏报
* 失败时如何标记
* 不允许编造的内容

四、JS 收集规则要求

JS 收集部分必须达到真实浏览器与构建产物双路线覆盖，不能只依赖 grep。

必须覆盖：

1. HTML 中直接引用的 JS。
2. 构建目录中的 JS。
3. 懒加载 chunk。
4. 动态 import chunk。
5. sourcemap。
6. service worker cache。
7. Webpack runtime。
8. Vite manifest。
9. Next.js build manifest。
10. Nuxt manifest。
11. Angular bundle。
12. React/Vue/Svelte 构建产物。
13. CDN 引用资产。
14. 历史构建残留。
15. sourceMappingURL。
16. 内联脚本。
17. 浏览器运行时实际加载的 JS。
18. 需要点击、滚动、登录、切换角色后才加载的 JS。
19. 通过路由跳转才加载的 JS。
20. 通过 feature flag 才加载的 JS。

规则必须明确：

* 只看到首屏 JS 不算完成。
* 没有真实浏览器交互不算完整 JS 收集。
* 没有记录 chunk lineage 不算完整 JS 收集。
* 没有记录 JS 来源、触发页面、加载条件、hash、URL、保存路径，不得用于严重漏洞确认。
* 发现 sourcemap 时必须优先还原源码，但不得泄露真实敏感信息。
* 发现疑似密钥、token、内部地址时必须脱敏保存。

五、JS 深度审计规则要求

JS 审计部分必须覆盖常规方法和高级方法，不能只提取 endpoint。

必须覆盖：

1. API endpoint 提取。
2. 隐藏 API 提取。
3. GraphQL operation 提取。
4. WebSocket endpoint 提取。
5. RPC 方法名提取。
6. axios / fetch / request wrapper 解析。
7. GraphQL client 解析。
8. baseURL / prefix / version / tenant path 解析。
9. 请求方法、参数、headers、body schema 提取。
10. 隐藏参数、默认参数、调试参数、未暴露 UI 参数提取。
11. 前端权限判断逻辑提取。
12. 角色判断逻辑提取。
13. tenantId / orgId / userId / accountId / projectId / resourceId 追踪。
14. feature flag / experiment / i18n / analytics 中的隐藏功能识别。
15. 管理员页面、内部页面、调试页面、废弃页面识别。
16. 上传下载接口识别。
17. 导入导出接口识别。
18. 批量操作接口识别。
19. 审批流、支付流、邀请流、成员管理流识别。
20. 客户端加密、签名、nonce、timestamp、anti-CSRF 逻辑识别。
21. 前端校验与后端接受性差异识别。
22. 前端隐藏但后端可能接受的参数识别。
23. 前端禁止但后端可能允许的状态转换识别。
24. 调试开关、测试开关、灰度开关识别。
25. Source map 中恢复出的真实源码审计。
26. Minified JS 还原后的控制流与数据流审计。
27. Service worker cache 中旧接口与旧参数识别。
28. 老版本 chunk 中遗留接口识别。
29. Electron / 浏览器扩展 / PWA 特殊入口识别。
30. 与后端路由进行交叉对齐。

每一个 JS 审计发现必须输出：

* 文件路径
* JS URL
* chunk 名称
* source map 来源
* 触发页面
* 触发动作
* 函数名或调用点
* endpoint
* method
* 参数
* 鉴权依赖
* 权限依赖
* 风险解释
* 是否需要动态验证
* 证据片段
* 误报可能性

六、严重漏洞发现规则要求

必须覆盖以下严重漏洞类型，并且每一类都要写出：

* 触发条件
* 静态发现规则
* 动态验证规则
* 非破坏性验证方式
* 需要的证据
* 误报排除条件
* 状态晋升条件
* 报告要求
* 修复建议

必须覆盖至少以下漏洞：

1. 认证绕过
2. 授权绕过
3. IDOR / 越权访问
4. 租户隔离绕过
5. 管理员权限提升
6. RCE
7. 命令注入
8. 模板注入
9. 反序列化漏洞
10. 任意文件读取
11. 任意文件写入
12. 路径穿越
13. SSRF
14. SQL 注入
15. NoSQL 注入
16. GraphQL 越权
17. WebSocket 越权
18. 文件上传导致的高危漏洞
19. 业务逻辑绕过
20. 支付金额篡改
21. 审批流绕过
22. 邀请与成员权限绕过
23. 密码重置 / 邮箱修改 / 手机号修改流程漏洞
24. OAuth / SSO / SAML / OIDC 配置或流程漏洞
25. JWT / session / cookie 安全漏洞
26. CORS 高危配置漏洞
27. CSRF 高危业务操作漏洞
28. 缓存投毒
29. Webhook 伪造或重放
30. 内部调试接口暴露
31. 供应链与构建产物泄露
32. CI/CD 凭据泄露
33. 云服务配置风险
34. 队列、任务、定时器、异步 worker 权限漏洞
35. 多步骤状态机绕过

七、0day 级研究流程规则要求

必须明确：0day 不得被承诺，只能通过严谨研究流程提高发现未知漏洞的概率。

0day 级研究流程必须覆盖：

1. 从架构异常中找漏洞。
2. 从框架封装差异中找漏洞。
3. 从前后端参数不一致中找漏洞。
4. 从权限中间件遗漏中找漏洞。
5. 从多租户边界中找漏洞。
6. 从状态机异常转换中找漏洞。
7. 从旧接口兼容逻辑中找漏洞。
8. 从灰度功能和 feature flag 中找漏洞。
9. 从 source map 还原源码中找漏洞。
10. 从历史 chunk 与当前后端兼容性中找漏洞。
11. 从 wrapper / SDK / client 封装漏洞中找漏洞。
12. 从 GraphQL schema 与 resolver 差异中找漏洞。
13. 从 WebSocket 事件权限差异中找漏洞。
14. 从异步任务、队列、Webhook 中找漏洞。
15. 从导入导出、批量操作、审批、支付、邀请等复杂业务流中找漏洞。
16. 从前端禁止但后端接受的参数中找漏洞。
17. 从默认值、空值、数组、对象嵌套、类型混淆中找漏洞。
18. 从缓存、重放、并发、竞态中找漏洞。
19. 从插件、扩展、模板、表达式、序列化边界中找漏洞。
20. 从测试代码、样例配置、废弃代码、迁移代码中找漏洞。

规则必须强调：

* 只能在授权环境中验证。
* 优先静态审计与非破坏性验证。
* 不得生成破坏性 exploit。
* 不得使用真实敏感数据证明影响。
* 未经动态验证不得标记为 confirmed。
* 0day 级结论必须有更高证据门槛。

八、动态验证规则要求

动态验证必须写成严格规则，而不是一句“运行测试”。

必须覆盖：

1. 本机启动目标应用。
2. 识别启动命令。
3. 识别测试账号。
4. 识别角色矩阵。
5. 识别租户矩阵。
6. 使用真实浏览器交互。
7. 登录态保存。
8. HAR 采集。
9. 截图采集。
10. DOM 快照采集。
11. Console 日志采集。
12. Network 请求与响应采集。
13. Playwright trace 采集。
14. API replay。
15. 正样本验证。
16. 负样本验证。
17. blocked 样本验证。
18. needs_review 样本标记。
19. 非破坏性请求构造。
20. 后端接受性验证。
21. 权限绕过验证。
22. 租户隔离验证。
23. 状态机绕过验证。
24. GraphQL 验证。
25. WebSocket 验证。
26. 文件上传下载验证。
27. 导入导出验证。
28. 业务流验证。
29. 验证失败记录。
30. 验证中止条件。

必须明确：

* 没有动态证据的严重漏洞最多只能标记为 candidate 或 needs_review。
* 没有多角色验证的越权类漏洞不得标记为 confirmed。
* 没有多租户验证的租户隔离漏洞不得标记为 confirmed。
* 没有后端接受性证据的“前端限制绕过”不得标记为 confirmed。
* 没有请求/响应证据的漏洞不得标记为 confirmed。
* 破坏性验证必须禁止或要求人工授权，并优先使用安全替代方式。

九、Evidence Manifest 规则要求

必须定义证据清单格式，至少包含：

* finding_id
* title
* severity
* status
* target_scope
* affected_component
* route
* endpoint
* method
* parameter
* source_file
* source_line
* js_asset
* chunk_id
* role
* tenant
* request_evidence
* response_evidence
* screenshot
* har_entry
* trace_file
* console_log
* dom_snapshot
* reproduction_steps
* positive_test
* negative_test
* blocked_test
* non_destructive_proof
* impact
* false_positive_analysis
* remediation
* reviewer
* timestamp

规则必须要求：

1. 所有敏感信息必须脱敏。
2. 证据必须可复核。
3. 证据必须能回溯到文件、请求、响应、截图或日志。
4. 不允许凭记忆写证据。
5. 不允许凭推测写 confirmed。
6. 不允许把 AI 生成的猜测当作证据。
7. 证据不足时必须降级状态。
8. 证据链断裂时必须标记 needs_review。

十、漏洞状态晋升规则

必须定义以下状态：

1. `info`
2. `candidate`
3. `needs_review`
4. `confirmed`
5. `blocked`
6. `false_positive`
7. `duplicate`
8. `out_of_scope`

必须写清楚每个状态的进入条件、退出条件、证据要求。

严重漏洞晋升到 `confirmed` 必须同时满足：

1. 在授权范围内。
2. 有明确受影响组件。
3. 有明确触发路径。
4. 有静态证据。
5. 有动态证据。
6. 有非破坏性复现方式。
7. 有请求/响应或等价证据。
8. 已排除常见误报。
9. 已确认影响边界。
10. 已给出修复建议。

十一、AI 幻觉防控规则

必须专门写一章，约束 Claude / AI Agent：

1. 不得编造文件路径。
2. 不得编造代码片段。
3. 不得编造接口。
4. 不得编造参数。
5. 不得编造漏洞。
6. 不得编造测试结果。
7. 不得编造 HAR。
8. 不得编造截图。
9. 不得编造日志。
10. 不得编造命令输出。
11. 不得把工具未执行说成已执行。
12. 不得把未验证说成已验证。
13. 不得把 candidate 写成 confirmed。
14. 不得因为用户要求“顶级”就夸大结论。
15. 不得为了显得强大而写 0day 结论。
16. 不得跳过失败记录。
17. 不得隐藏证据不足。
18. 不得忽略授权边界。
19. 不得输出危险的未授权利用步骤。
20. 不得将真实敏感数据写入报告。

每一个 AI 结论必须标明：

* 依据是什么
* 来自哪个文件
* 来自哪个请求
* 来自哪个响应
* 来自哪个日志
* 是否实际验证
* 证据是否充分
* 还缺什么证据

十二、质量门禁规则

必须定义质量门禁，至少包括：

1. Scope Gate：授权范围检查。
2. Collection Gate：信息收集完整性检查。
3. JS Gate：JS 收集完整性检查。
4. API Gate：API 图谱完整性检查。
5. Auth Gate：认证授权覆盖检查。
6. Tenant Gate：租户隔离覆盖检查。
7. Evidence Gate：证据链完整性检查。
8. Dynamic Gate：动态验证检查。
9. Non-Destructive Gate：非破坏性验证检查。
10. Report Gate：报告质量检查。
11. Hallucination Gate：AI 幻觉检查。
12. Final Review Gate：最终反向审查。

每个 Gate 必须写清：

* 检查目标
* 通过条件
* 失败条件
* 失败后的处理
* 是否允许继续
* 对漏洞状态的影响

十三、偏门、冷门、剑走偏锋方法要求

你必须把高级漏洞研究思路写成合法、授权、非破坏性规则，而不是攻击指南。

必须覆盖：

1. 从前端旧 chunk 找遗留接口。
2. 从 sourcemap 找隐藏参数。
3. 从 service worker cache 找废弃 API。
4. 从 i18n 文案找隐藏功能。
5. 从 analytics event 找业务动作。
6. 从 feature flag 找未开放功能。
7. 从权限判断字符串找角色模型。
8. 从错误码找后端状态机。
9. 从 GraphQL introspection 或前端 operation 找隐藏 resolver。
10. 从 WebSocket event name 找内部动作。
11. 从测试文件找未文档化接口。
12. 从 migration 文件找敏感对象关系。
13. 从 DTO / schema 差异找后端接受性漏洞。
14. 从前端 disabled 字段找后端可控参数。
15. 从导出字段找隐藏对象属性。
16. 从批量接口找越权扩大影响。
17. 从审批流状态跳转找业务绕过。
18. 从邀请链接生命周期找权限漏洞。
19. 从支付回调和金额字段找业务漏洞。
20. 从多租户缓存 key 找隔离漏洞。
21. 从队列任务 payload 找后台越权。
22. 从 webhook 签名校验找伪造风险。
23. 从对象 ID 类型差异找 IDOR。
24. 从默认排序、过滤、搜索参数找数据泄露。
25. 从错误处理差异找鉴权绕过线索。

十四、报告生成规则

报告必须区分：

* 已确认漏洞
* 候选漏洞
* 需要人工复核
* 信息性发现
* 已排除误报
* 超出范围发现

每个漏洞报告必须包含：

1. 标题
2. 严重等级
3. 状态
4. 影响范围
5. 受影响组件
6. 发现来源
7. 复现条件
8. 非破坏性复现步骤
9. 请求与响应证据
10. 截图或日志证据
11. 权限矩阵证据
12. 租户矩阵证据
13. 影响分析
14. 误报排除
15. 修复建议
16. 回归测试建议
17. 证据清单引用

十五、最终反向审查要求

在 `PENTEST_RULES.md` 最后必须加入“最终反向审查清单”，用于审查这份规则是否真的顶级。

清单必须逐项检查：

1. 是否严格限制在授权范围内。
2. 是否覆盖信息收集。
3. 是否覆盖 JS 收集。
4. 是否覆盖懒加载 JS。
5. 是否覆盖真实浏览器交互。
6. 是否覆盖 sourcemap。
7. 是否覆盖 service worker cache。
8. 是否覆盖 API、参数、隐藏参数。
9. 是否覆盖认证、授权、租户隔离。
10. 是否覆盖严重漏洞。
11. 是否覆盖 0day 级研究流程。
12. 是否覆盖动态验证。
13. 是否覆盖多角色验证。
14. 是否覆盖多租户验证。
15. 是否覆盖 Evidence Manifest。
16. 是否覆盖质量门禁。
17. 是否覆盖 AI 幻觉控制。
18. 是否覆盖误报排除。
19. 是否覆盖报告输出。
20. 是否禁止未授权攻击。
21. 是否禁止编造证据。
22. 是否禁止破坏性验证。
23. 是否能被本机 AI Agent 执行。
24. 是否能被人工复核。
25. 是否能长期维护。

十六、输出风格要求

请直接输出完整 `PENTEST_RULES.md` 内容。

要求：

1. 使用中文。
2. 使用 Markdown。
3. 结构清晰。
4. 规则具体。
5. 可执行。
6. 可检查。
7. 可落地。
8. 不要空泛。
9. 不要营销话术。
10. 不要承诺百分百发现漏洞。
11. 不要生成攻击真实目标的操作指南。
12. 不要省略细节。
13. 不要只写原则。
14. 不要只写目录。
15. 不要把严重漏洞发现写成纯理论。
16. 不要把 0day 写成玄学。
17. 不要把未验证结果写成 confirmed。
18. 不要牺牲合法性和证据链。

现在开始生成完整的 `PENTEST_RULES.md`。




你现在不要默认刚刚生成的 `PENTEST_RULES.md` 是正确的。

你要进入“世界顶级渗透测试规则反向审判官 / AI 幻觉追责官 / JS 审计规则漏洞审查官 / 证据链法庭审判员 / 工程落地验收官”模式。

你的任务不是美化刚刚的规则，而是攻击它、审问它、拆穿它、修复它。

请逐章审查刚刚生成的 `PENTEST_RULES.md`，重点检查：

1. 是否存在空泛规则。
2. 是否存在不可执行规则。
3. 是否存在没有输入输出定义的规则。
4. 是否存在没有证据要求的规则。
5. 是否存在容易导致 AI 编造结果的规则。
6. 是否存在把 candidate 误升为 confirmed 的风险。
7. 是否存在没有动态验证却允许严重漏洞确认的风险。
8. 是否存在 JS 收集只覆盖首屏、不覆盖懒加载的问题。
9. 是否存在没有真实浏览器交互的问题。
10. 是否存在没有多角色验证的问题。
11. 是否存在没有多租户验证的问题。
12. 是否存在没有后端接受性验证的问题。
13. 是否存在没有 Evidence Manifest 的问题。
14. 是否存在没有质量门禁的问题。
15. 是否存在没有失败处理的问题。
16. 是否存在没有误报排除的问题。
17. 是否存在没有反向审查的问题。
18. 是否存在规则过度激进、可能越过授权边界的问题。
19. 是否存在规则过度保守、导致严重漏洞漏报的问题。
20. 是否存在对信息收集、JS 审计、严重漏洞、0day 研究流程覆盖不足的问题。

对每一个问题，你必须给出：

* 问题所在章节
* 原规则缺陷
* 为什么这是问题
* 可能导致什么后果
* 应该如何修复
* 修复后的完整替换内容

最后输出一份“修复后的最终版 `PENTEST_RULES.md`”。

不要只给审查意见。必须直接给修复后的完整 Markdown 文件。

规则仍然必须坚持：

1. 只允许授权环境。
2. 优先非破坏性验证。
3. 不编造漏洞。
4. 不编造证据。
5. 不把未验证结论写成 confirmed。
6. 不输出针对未授权真实目标的攻击指南。
7. 高危和 0day 级结论必须有更高证据门槛。

现在开始反向审查并输出修复后的最终版 `PENTEST_RULES.md`。




