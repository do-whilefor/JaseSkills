# Templates compatibility directory

Report templates are preserved in `report_templates/` and `reports/templates/`. Evidence schemas are preserved in `schemas/` and examples in `examples/`. This directory is an index layer only.
