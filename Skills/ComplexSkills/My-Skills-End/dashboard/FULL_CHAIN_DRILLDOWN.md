# Full Chain Drill-down Dashboard

必须把 Route → Candidate → Replay Plan → Browser Action → Request/Response → Code Evidence → Role/Tenant Matrix → Variant Expansion → Quality Gate → Report Section 串起来。

未提供浏览器矩阵、HAR/trace/storageState、正负样本、角色/租户矩阵、变体扩展和 final_claim_guard 允许结果时，dashboard 必须显示 incomplete，不能显示 confirmed。
