你现在不是普通文档整理助手，而是这个 TXT 转 Claude Skills 项目的最终责任人。

我要你记住：如果你把 TXT 做成了摘要、空壳、泛泛而谈的说明书，或者生成一堆看起来复杂但实际无法使用的 Skills，那就是失败。你的任务不是“表现得很专业”，而是把 TXT 文档复刻成一套真正能被 Claude 调用、能落地、能验收、能追溯原文的 Skills。

我会给你一个 TXT 文档。你必须基于这个 TXT 做 Skills。

核心要求：

1. Skills 个数不要太多。
2. 默认只做 1 个主 Skill。
3. 只有在 TXT 内容明显包含多个完全独立主题时，才允许拆成 2 到 3 个 Skills。
4. 禁止为了显得高级而拆出一堆无用 Skills。
5. 每个 Skill 都必须能解释清楚：它对应 TXT 的哪一部分、解决什么问题、输入是什么、输出是什么、如何验收。
6. 如果一个 Skill 不能独立执行，就不要单独拆出来。
7. 文件夹命名必须简洁，但必须突出 TXT 文件名或 TXT 核心主题。
8. 不允许使用空泛名字，例如 `best-skill`、`final-skill`、`advanced-skill`、`audit-skill`、`new-skill`。
9. 中文 TXT 文件名必须提取核心含义，转成简洁英文短横线命名。
10. 命名必须做到“一眼能看出来自哪个 TXT”。

输入 TXT：

【在这里粘贴 TXT 文件路径或 TXT 全文】

你的工作方式必须如下。

第一步：完整理解 TXT。

你必须逐段读取 TXT，不能只看标题，不能只提炼大意。你要提取：

* 原文主题
* 原文目标
* 原文流程
* 原文规则
* 原文禁止事项
* 原文检查项
* 原文输出要求
* 原文角色设定
* 原文隐含约束
* 原文中适合转成模板的内容
* 原文中适合转成 checklist 的内容
* 原文中适合转成 workflow 的内容
* 原文中容易被 AI 忽略的细节

你必须把 TXT 原文转成可执行的 Skill，而不是把 TXT 写成读后感。

第二步：决定 Skill 数量。

你必须先判断：

```text
是否只需要 1 个主 Skill？
是否存在必须拆分的独立主题？
拆分是否会让使用更清晰？
拆分是否会导致目录臃肿？
```

默认结论必须倾向于：

```text
1 个主 Skill + 少量 templates / checklists / examples / tests
```

只有满足以下条件，才允许拆分多个 Skills：

```text
1. TXT 中存在两个或多个完全不同的任务目标；
2. 这些任务的输入输出完全不同；
3. 合并会导致 Claude 调用混乱；
4. 拆分后每个 Skill 都能独立运行；
5. 拆分不会导致重复文件和空壳文件。
```

否则，必须保持一个主 Skill。

第三步：设计最小但完整的目录结构。

优先使用这个结构：

```text
<skill-name>/
  SKILL.md
  README.md
  templates/
    output-template.md
  checklists/
    quality-gate.md
    final-review.md
  examples/
    basic-example.md
    full-example.md
  tests/
    skill-quality-tests.md
```

只有 TXT 明确需要时，才可以增加：

```text
rules/
workflows/
schemas/
fixtures/
```

禁止无理由增加目录。

第四步：写 SKILL.md。

`SKILL.md` 必须是核心，不能写成空泛说明。

必须包含：

```markdown
# Skill 名称

## 适用范围

## 不适用范围

## 输入要求

## 输出要求

## 原文复刻规则

## 工程化补强规则

## 核心工作流

## 分阶段执行步骤

## 质量门禁

## 幻觉控制

## 失败处理

## 输出格式

## 自检清单

## TXT 到 Skill 映射说明
```

每一节都必须可执行。

禁止出现以下无效表达，除非后面给出具体执行步骤：

* 深入分析
* 全面检查
* 尽可能
* 结合上下文
* 视情况
* 建议优化
* 可以考虑
* 进一步完善
* 高质量输出
* 世界顶级
* 专业化处理

如果你写了“分析”，必须说明：

```text
分析什么
从哪里分析
输出什么
判断标准是什么
失败怎么办
```

如果你写了“验证”，必须说明：

```text
验证对象
验证步骤
验证证据
通过标准
失败处理
```

如果你写了“报告”，必须说明：

```text
字段
格式
结论等级
证据字段
禁止下结论的情况
```

第五步：保留原文，标记补强。

你必须严格区分两类内容：

```text
原文复刻：直接来自 TXT 的规则、流程、要求、结构、术语。
工程化补强：为了让 Skill 可执行而新增的输入、输出、质量门禁、测试、模板。
```

不允许把你新增的内容伪装成 TXT 原文。

必须输出映射表：

```markdown
| TXT 原文位置/标题 | Skill 文件 | 转化方式 | 原文复刻/工程化补强 | 备注 |
|---|---|---|---|---|
```

第六步：必须提供模板、清单、示例、测试。

最少文件：

```text
templates/output-template.md
checklists/quality-gate.md
checklists/final-review.md
examples/basic-example.md
examples/full-example.md
tests/skill-quality-tests.md
```

模板必须有实际字段，不能只有标题。

checklist 必须是可勾选格式：

```markdown
- [ ] 检查项
```

测试必须能检查这些问题：

```text
1. 是否漏掉 TXT 关键内容
2. 是否把摘要当复刻
3. 是否加入无关内容
4. 是否命名过长或空泛
5. 是否目录臃肿
6. 是否没有输入输出定义
7. 是否没有质量门禁
8. 是否没有失败处理
9. 是否没有 TXT 映射
10. 是否把工程化补强伪装成原文
```

第七步：进入追责反查模式。

初版完成后，你必须用最严格标准反查自己。

你要假设：

```text
如果这个 Skill 交付后不能直接使用，就是你的设计失败。
如果 Claude 调用 Skill 后仍然只会总结 TXT，就是你的规则失败。
如果 Skill 文件很多但没有实际作用，就是你的架构失败。
如果命名看不出来源 TXT，就是你的命名失败。
如果输出不能追溯 TXT，就是你的复刻失败。
如果没有质量门禁，就是你的验收失败。
如果没有测试，就是你的工程化失败。
```

你必须逐项检查：

```markdown
- [ ] 是否真的复刻 TXT，而不是总结？
- [ ] 是否保留 TXT 的关键术语？
- [ ] 是否保留 TXT 的关键流程？
- [ ] 是否保留 TXT 的关键约束？
- [ ] 是否错误扩展了 TXT 没有的能力？
- [ ] 是否 Skill 数量过多？
- [ ] 是否存在空壳 Skill？
- [ ] 是否命名简洁？
- [ ] 是否命名能对应 TXT 文件名？
- [ ] 是否每个文件都有明确作用？
- [ ] 是否模板可直接填写？
- [ ] 是否 checklist 可验收？
- [ ] 是否 examples 贴近 TXT？
- [ ] 是否 tests 能发现问题？
- [ ] 是否 SKILL.md 能直接被 Claude 使用？
```

发现任何问题，你必须直接修复，不允许只写“建议后续优化”。

第八步：最终输出格式。

你必须按这个格式输出：

````markdown
# 1. TXT 理解结果

# 2. Skill 数量决策

说明为什么只做 1 个 Skill，或者为什么必须拆成 2 到 3 个。

# 3. 推荐 Skill 名称

# 4. 命名理由

# 5. 最终目录结构

# 6. 每个文件的完整内容

## <skill-name>/SKILL.md

```markdown
完整内容
````

## <skill-name>/README.md

```markdown
完整内容
```

## <skill-name>/templates/output-template.md

```markdown
完整内容
```

## <skill-name>/checklists/quality-gate.md

```markdown
完整内容
```

## <skill-name>/checklists/final-review.md

```markdown
完整内容
```

## <skill-name>/examples/basic-example.md

```markdown
完整内容
```

## <skill-name>/examples/full-example.md

```markdown
完整内容
```

## <skill-name>/tests/skill-quality-tests.md

```markdown
完整内容
```

# 7. TXT 到 Skill 映射表

# 8. 追责反查结果

# 9. 修复后的最终版本说明

# 10. Windows PowerShell 落地命令

给出创建目录和写入文件的 PowerShell 命令。

```

第九步：最终硬性标准。

最终结果必须满足：

1. Skill 数量少而精。
2. 默认 1 个主 Skill。
3. 不堆空目录。
4. 不堆空文件。
5. 不做摘要版。
6. 不做概念版。
7. 不做口号版。
8. 不做无法执行的说明书。
9. 必须复刻 TXT。
10. 必须追溯 TXT。
11. 必须有模板。
12. 必须有 checklist。
13. 必须有 examples。
14. 必须有 tests。
15. 必须有质量门禁。
16. 必须有失败处理。
17. 必须区分原文复刻和工程化补强。
18. 必须给出 PowerShell 落地命令。
19. 命名必须简洁。
20. 命名必须突出 TXT 文件名或核心主题。

现在开始执行。

不要把这个任务做成“整理文档”。  
不要把这个任务做成“总结 TXT”。  
不要把这个任务做成“看起来很高级的空壳 Skills”。  
你要交付的是能直接保存、能直接调用、能直接验收、能追溯 TXT 原文的 Claude Skills。
```






你刚刚已经根据 TXT 文档生成了一套 Claude Skills。现在不要默认刚刚的结果是正确的，也不要继续美化它。

你现在切换为：Skills 交付前最终审查官、TXT 复刻一致性审判员、Claude Skills 工程化验收官、AI 幻觉追责官、空壳文件清理员。

你的任务不是夸奖刚刚的 Skills，而是反向审查、拆穿问题、压缩冗余、修复失败点，最后给出真正可交付的最终版。

一、先重新读取输入材料

你必须重新读取：

1. 原始 TXT 文档。
2. 你刚刚生成的 Skills 全部文件。
3. 目录结构。
4. 文件命名。
5. SKILL.md。
6. templates。
7. checklists。
8. examples。
9. tests。
10. TXT 到 Skill 映射表。

如果你无法重新读取某个文件，必须明确标记为：

```text
未验证，不得宣称已通过。
```

二、核心反思问题

请逐项回答，不允许空泛回答：

1. 我刚刚是否只是总结了 TXT，而不是复刻 TXT？
2. 我是否遗漏了 TXT 的关键规则、流程、术语、边界或输出要求？
3. 我是否把 TXT 原文没有的内容当成了原文？
4. 我是否把“工程化补强”伪装成了“原文复刻”？
5. 我是否为了显得高级，拆出了过多 Skills？
6. 是否应该合并成 1 个主 Skill？
7. 是否存在空壳目录或空壳文件？
8. 文件夹命名是否简洁？
9. 文件夹命名是否能突出 TXT 文件名或核心主题？
10. SKILL.md 是否真的能被 Claude 直接调用？
11. templates 是否有可填写字段？
12. checklists 是否能实际验收？
13. examples 是否贴近 TXT 原文主题？
14. tests 是否能发现漏复刻、幻觉扩展、空壳文件、命名失败？
15. 是否存在“看起来专业但不可执行”的内容？
16. 是否有明确输入、输出、失败处理、质量门禁？
17. 是否每个文件都能追溯到 TXT 原文或工程化补强理由？

三、Skills 数量压缩审查

请强制执行以下判断：

```text
默认只保留 1 个主 Skill。
只有当 TXT 中存在多个完全独立任务，并且输入输出完全不同，才允许保留 2 到 3 个 Skills。
超过 3 个 Skills 必须给出不可反驳的理由，否则立即合并。
```

你必须检查：

1. 是否存在多个 Skills 做同一件事？
2. 是否存在只改名字但内容重复的 Skills？
3. 是否存在不能独立运行的 Skills？
4. 是否存在只包含概念说明的 Skills？
5. 是否存在拆分后反而增加 Claude 调用成本的 Skills？
6. 是否存在目录复杂但收益很低的问题？

处理规则：

* 能合并就合并。
* 能删除就删除。
* 能放进 templates/checklists/examples/tests 的内容，不要单独做 Skill。
* 不要用多个 Skills 掩盖主 Skill 质量不足。
* 最终结构必须少而强。

四、TXT 复刻一致性审查

请建立逐项映射表：

```markdown
| TXT 原文位置/标题 | 原文关键内容 | 当前 Skill 位置 | 是否完整复刻 | 是否被改写失真 | 是否遗漏 | 修复动作 |
|---|---|---|---|---|---|---|
```

审查标准：

1. TXT 每个关键标题都必须有对应位置。
2. TXT 每条核心规则都必须能在 Skill 中找到。
3. TXT 的禁止事项不能被弱化。
4. TXT 的流程不能被打乱到无法执行。
5. TXT 的输出要求不能被改成泛泛报告。
6. TXT 的边界要求必须转成 hard rules。
7. TXT 的检查项必须转成 checklist 或 quality gate。
8. TXT 的模板型内容必须转成 template。
9. TXT 的示例型内容必须转成 examples。
10. TXT 没有的内容不得伪装成原文。

五、命名审查

逐项检查命名：

1. Skill 文件夹名是否过长？
2. 是否包含无效词，例如 best、final、new、advanced、ultimate、skill-only？
3. 是否能看出对应 TXT 文件名？
4. 中文 TXT 文件名是否正确提取了核心含义？
5. 是否使用小写英文和短横线？
6. 是否存在多个名字表达同一功能？
7. 文件名是否简洁但有区分度？
8. 是否有 `README-final.md`、`new-template.md`、`copy.md` 这种低质量命名？

如果命名失败，必须直接重命名，并更新所有引用。

六、文件可执行性审查

逐个文件检查：

```markdown
| 文件路径 | 文件作用 | 是否必要 | 是否空壳 | 是否重复 | 是否可执行 | 是否能追溯 TXT | 处理动作 |
|---|---|---|---|---|---|---|---|
```

处理规则：

1. 空壳文件删除。
2. 重复文件合并。
3. 没有调用价值的文件删除。
4. 只有标题没有字段的模板重写。
5. 只有口号的 checklist 重写。
6. 不能发现问题的 tests 重写。
7. 不能支撑 Claude 使用的 README 重写。
8. SKILL.md 不完整则重写。

七、SKILL.md 严格审判

检查 SKILL.md 是否包含：

```markdown
- [ ] 适用范围
- [ ] 不适用范围
- [ ] 输入要求
- [ ] 输出要求
- [ ] 原文复刻规则
- [ ] 工程化补强规则
- [ ] 核心工作流
- [ ] 分阶段执行步骤
- [ ] 质量门禁
- [ ] 幻觉控制
- [ ] 失败处理
- [ ] 输出格式
- [ ] 自检清单
- [ ] TXT 到 Skill 映射说明
```

如果某项缺失，必须补齐。

禁止以下无效表达单独出现：

```text
深入分析
全面检查
尽可能
视情况
建议优化
可以考虑
进一步完善
高质量输出
专业化处理
世界顶级
```

如果出现这些词，必须改成具体动作、输入、输出、验收标准。

八、质量门禁审查

必须检查当前 Skills 是否有明确 gate：

1. 未读取 TXT 不得生成。
2. 未建立映射表不得通过。
3. 未区分原文复刻和工程化补强不得通过。
4. 未覆盖关键章节不得通过。
5. 未提供模板不得通过。
6. 未提供 checklist 不得通过。
7. 未提供 examples 不得通过。
8. 未提供 tests 不得通过。
9. 未定义失败处理不得通过。
10. 文件命名无法对应 TXT 不得通过。
11. Skill 数量无理由过多不得通过。
12. 空壳文件存在不得通过。
13. 输出不可追溯 TXT 不得通过。

缺失的 gate 必须补齐到 `checklists/quality-gate.md` 和 `SKILL.md`。

九、反幻觉审查

检查是否存在：

1. 没有 TXT 来源的新增功能。
2. 把推测当事实。
3. 把工程化补强说成原文要求。
4. 把未读取文件说成已读取。
5. 把候选结构说成最终结构。
6. 用高级词掩盖不可执行。
7. 用大量目录掩盖内容不足。
8. 示例脱离 TXT。
9. 测试无法检测真实问题。
10. 映射表过粗。

发现后必须修复。

十、最终修复要求

你不能只输出问题清单。你必须给出修复后的最终版，包括：

1. 最终 Skill 数量。
2. 最终 Skill 名称。
3. 最终目录结构。
4. 删除了哪些文件，为什么。
5. 合并了哪些文件，为什么。
6. 重命名了哪些文件，为什么。
7. 重写了哪些文件，为什么。
8. 修复后的每个文件完整内容。
9. 修复后的 TXT 到 Skill 映射表。
10. 修复后的质量门禁。
11. 修复后的 tests。
12. 最终自检结论。

十一、最终输出格式

请严格按以下格式输出：

````markdown
# 1. 重新审查范围

# 2. 原版本核心问题

# 3. Skill 数量压缩结论

# 4. 命名审查结论

# 5. 文件清理与合并记录

| 原路径 | 处理动作 | 原因 | 新路径 |
|---|---|---|---|

# 6. TXT 复刻一致性映射表

| TXT 原文位置/标题 | 原文关键内容 | 当前 Skill 位置 | 是否完整复刻 | 是否被改写失真 | 是否遗漏 | 修复动作 |
|---|---|---|---|---|---|---|

# 7. 修复后的最终目录结构

# 8. 修复后的完整文件内容

## <skill-name>/SKILL.md

```markdown
完整内容
````

## <skill-name>/README.md

```markdown
完整内容
```

## <skill-name>/templates/output-template.md

```markdown
完整内容
```

## <skill-name>/checklists/quality-gate.md

```markdown
完整内容
```

## <skill-name>/checklists/final-review.md

```markdown
完整内容
```

## <skill-name>/examples/basic-example.md

```markdown
完整内容
```

## <skill-name>/examples/full-example.md

```markdown
完整内容
```

## <skill-name>/tests/skill-quality-tests.md

```markdown
完整内容
```

# 9. 修复后的质量门禁

# 10. 修复后的测试覆盖说明

# 11. 最终自检结论

必须明确写：

* 是否保留 1 个主 Skill。
* 是否存在空壳文件。
* 是否存在无法追溯 TXT 的内容。
* 是否存在把工程化补强伪装成原文的问题。
* 是否所有文件都可直接落地。

```

十二、最后硬性要求

你必须以交付质量为准，不以输出长度为准。

如果原版本已经很好，也要证明它为什么好；如果无法证明，就必须修复。

不要说“建议后续优化”。  
不要说“可以进一步完善”。  
不要说“整体较好”。  
不要用表面反思代替实际修改。  
不要保留无用 Skills。  
不要保留空壳文件。  
不要让命名脱离 TXT。  
不要把摘要当复刻。  
不要把没有证据的内容写成结论。

现在开始反查，并输出修复后的最终可交付版本。
```
