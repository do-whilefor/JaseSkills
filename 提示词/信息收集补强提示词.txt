你现在不是普通代码审查助手，而是世界顶级安全渗透测试信息收集 Skill 审计专家、红队信息收集流程设计专家、AI 幻觉控制专家、证据链审计专家、脚本可执行性审计专家。

你的任务是审查我当前的信息收集 Skills，判断它是否真的能做到世界顶级的信息收集，而不是只停留在表面目录扫描、关键词搜索、普通端口信息、普通 README 分析。

安全边界：

1. 只允许在我明确授权的本机项目、代码仓库、测试环境、靶场或我提供的文件范围内工作。
2. 不允许对未授权公网目标进行主动扫描、爆破、攻击、绕过、持久化或破坏性测试。
3. 所有结论必须基于文件、代码、脚本、配置、模板、测试样例、命令输出或明确证据。
4. 禁止编造文件路径、禁止假设脚本可运行、禁止把“可能存在”写成“已经存在”。
5. 如果某项能力没有文件、脚本、规则、模板、测试或输出格式支撑，必须标记为 not implemented / weak / hallucination risk。

你需要完整审查以下内容：

一、整体结构审查

请先递归读取 Skills 目录结构，输出：

1. 所有 Skill 名称。
2. 每个 Skill 的入口文件。
3. 每个 Skill 的触发条件。
4. 每个 Skill 的核心能力。
5. 每个 Skill 调用的脚本、规则、模板、知识库、测试样例。
6. 是否存在空壳 Skill、重复 Skill、冲突 Skill、过期 Skill、描述强但能力弱的 Skill。
7. 是否存在 README / SKILL.md / manifest / scripts / templates / examples / tests 不一致的问题。

要求输出表格：

Skill | 文件路径 | 实际能力 | 声称能力 | 差距 | 证据 | 风险等级 | 修复建议

二、信息收集覆盖面审查

请判断当前 Skills 是否覆盖以下信息收集维度，并逐项给出证据路径：

1. 项目目录结构与模块边界。
2. 编程语言识别。
3. 框架识别。
4. 路由识别。
5. API 入口识别。
6. 控制器 / handler / service / model / middleware 关系识别。
7. 认证入口识别。
8. 鉴权逻辑识别。
9. 角色模型识别。
10. 多租户边界识别。
11. 参数来源识别。
12. 敏感参数识别。
13. 文件上传入口识别。
14. 文件读取入口识别。
15. 文件写入入口识别。
16. SSRF 相关 URL / webhook / callback / proxy / fetch / request 入口识别。
17. 命令执行入口识别。
18. 模板渲染入口识别。
19. SQL / NoSQL / ORM 查询入口识别。
20. GraphQL schema / resolver / introspection / operation 信息识别。
21. WebSocket / SSE / RPC / gRPC 入口识别。
22. 管理后台入口识别。
23. 调试接口识别。
24. 健康检查接口识别。
25. 文档接口识别，例如 Swagger、OpenAPI、Redoc、Postman collection。
26. CI/CD 配置识别。
27. Docker / Compose / Kubernetes / Helm / Terraform / Ansible 等部署信息识别。
28. 环境变量引用识别。
29. secrets / token / key / credential 风险线索识别。
30. 第三方服务识别，例如 S3、OSS、COS、Stripe、SendGrid、Twilio、Firebase、Supabase、Redis、MQ、Elasticsearch。
31. 域名、子域、回调域、CORS 域、OAuth redirect_uri 识别。
32. CSP、CORS、cookie、session、JWT、OAuth、SAML、OIDC 配置识别。
33. 日志、审计、监控、analytics、Sentry、Datadog、Grafana、Prometheus 信息识别。
34. 前端路由、前端接口、隐藏页面、feature flag、实验功能识别。
35. JS bundle、chunk、source map、service worker、manifest、webpack/vite/next build artifact 识别。
36. 移动端、桌面端、Electron、浏览器插件相关信息识别。
37. 测试文件、fixture、mock、seed、demo account、example config 识别。
38. 备份文件、旧接口、废弃接口、迁移脚本、临时文件识别。
39. 依赖漏洞信息与危险依赖配置识别。
40. 攻击面知识图谱生成能力。

每一项必须标记：

implemented / partial / missing / unverifiable

并给出：

证据文件路径、对应脚本、输出字段、失败场景、需要补充的规则或脚本。

三、隐藏信息收集能力审查

请重点审查当前 Skills 是否能发现普通信息收集容易漏掉的隐藏信息，包括但不限于：

1. 注释中的接口、token 名称、旧域名、测试账号线索。
2. source map 中泄露的原始源码路径、接口名、内部变量名。
3. minified JS 中的隐藏 API。
4. 前端 bundle 里的 GraphQL operation name。
5. service worker 缓存中的隐藏接口。
6. manifest 文件中的隐藏页面。
7. feature flag 控制的未开放功能。
8. CI/CD 中的部署域名、镜像仓库、secret 名称。
9. Docker / K8s 中的内部服务名、端口、环境变量。
10. Terraform / IaC 中的云资产、bucket、数据库、队列、VPC、IAM 线索。
11. 测试代码中的默认账号、角色、租户、权限模型。
12. seed / migration 中的管理员账号、默认组织、默认权限。
13. package script / Makefile / taskfile 中隐藏的调试命令。
14. OpenAPI / Swagger / Postman 中未在前端暴露的接口。
15. GraphQL schema 中隐藏 mutation。
16. WebSocket event name 与权限边界。
17. 后端路由中未被前端调用的接口。
18. 旧版本文档中的废弃接口。
19. 错误处理和日志中暴露的内部路径。
20. analytics / Sentry / source map 上传配置中的项目标识。
21. OAuth redirect_uri、CORS allowlist、trusted origins 中的子域。
22. 邮件模板、短信模板、通知模板中的隐藏链接。
23. 反向代理、Nginx、Apache、Traefik、Caddy 配置中的隐藏路径。
24. robots.txt、sitemap、well-known、security.txt 等授权范围内公开路径。
25. dev/staging/test/local 配置泄露的环境差异。

请输出：

隐藏信息类型 | 当前是否能收集 | 证据 | 漏洞关联价值 | 当前缺口 | 应该补充到哪个 Skill / 脚本 / 规则 / 模板

四、代码与脚本审查

请审查所有信息收集相关脚本：

1. 是否真的执行，而不是文档假装存在。
2. 是否有参数校验。
3. 是否有 scope 限制。
4. 是否默认非破坏性。
5. 是否支持 dry-run。
6. 是否支持 JSON 输出。
7. 是否支持 evidence manifest。
8. 是否有错误处理。
9. 是否有日志。
10. 是否有超时控制。
11. 是否有去重。
12. 是否有路径规范化。
13. 是否避免扫描授权范围外路径。
14. 是否避免把 secret 原文写进报告。
15. 是否支持大项目增量扫描。
16. 是否支持多语言项目。
17. 是否支持前后端混合项目。
18. 是否有测试样例。
19. 是否有负样本。
20. 是否有回放样本。

对每个脚本输出：

脚本路径 | 作用 | 输入 | 输出 | 是否可运行 | 依赖 | 缺陷 | 证据 | 修复建议

五、Markdown / 文档审查

请审查所有 md 文件：

1. 是否只写理念，没有操作步骤。
2. 是否存在过度承诺。
3. 是否存在“顶级”“全面”“自动发现”但没有实现支撑。
4. 是否存在与脚本实际能力不一致。
5. 是否存在重复、冲突、过期内容。
6. 是否有明确输入输出。
7. 是否有失败处理。
8. 是否有证据要求。
9. 是否有质量门槛。
10. 是否有人工复核条件。

输出：

文件路径 | 问题段落 | 问题类型 | 具体风险 | 修复方式 | 应补充内容

六、信息收集质量评分

请从 0 到 100 给当前 Skills 打分，并拆分为：

1. 授权边界清晰度。
2. 项目结构识别能力。
3. 语言 / 框架识别能力。
4. 路由 / API 识别能力。
5. 前端 JS 信息收集能力。
6. 后端信息收集能力。
7. 配置 / 部署 / CI 信息收集能力。
8. 云资产 / 第三方服务识别能力。
9. 认证 / 鉴权 / 角色 / 租户信息收集能力。
10. 隐藏信息发现能力。
11. 脚本可执行性。
12. 输出证据质量。
13. 去重与归一化能力。
14. 知识图谱能力。
15. 报告模板质量。
16. 测试覆盖率。
17. 幻觉控制能力。
18. 可维护性。
19. 可扩展性。
20. 真实项目适配能力。

每一项必须说明扣分原因和修复优先级。

七、最终输出格式

最后必须输出以下内容：

1. 总评分。
2. P0 必修问题。
3. P1 高优先级问题。
4. P2 增强问题。
5. 当前 Skills 最大的 10 个信息收集盲区。
6. 最容易导致漏掉高危漏洞的 10 个缺陷。
7. 最容易导致 AI 幻觉的 10 个缺陷。
8. 应新增的脚本清单。
9. 应新增的规则清单。
10. 应新增的模板清单。
11. 应新增的测试样例清单。
12. 文件级修复路线图。

不要泛泛而谈。每一个结论都必须绑定具体文件、证据、失败场景和修复动作。








你现在进入“反向审判与自我追责模式”。

你刚刚已经对我的信息收集 Skills 做了一轮审查。现在不要默认你的审查是正确的，不要维护自己的结论，不要补漂亮话。你要作为更苛刻的审计官，专门攻击你刚才的评估结果，找出其中所有可能的幻觉、漏报、误判、证据不足、路径缺失、脚本误读、文档误读、信息收集盲区。

你的目标是回答一个问题：

这个信息收集 Skills 是否真的可以做到世界顶级？如果不能，具体失败在哪里？

一、逐条反查你的每一个结论

对你刚才提出的每一个问题，逐条检查是否同时具备：

1. 具体文件路径。
2. 具体代码、脚本、规则、模板或 md 段落。
3. 具体失败原因。
4. 具体安全影响。
5. 具体漏收集的信息类型。
6. 可能导致漏掉的漏洞类型。
7. 修复后应该写入哪个文件。
8. 是否有测试样例验证。
9. 是否有 evidence manifest 记录。
10. 是否有可复现命令。

如果缺任意一项，必须把该结论降级为“证据不足”。

二、检查你是否漏掉了信息收集核心维度

重新检查是否覆盖：

1. 代码结构信息。
2. 脚本信息。
3. Markdown 文档信息。
4. 配置文件信息。
5. 构建产物信息。
6. 前端 JS 信息。
7. 后端路由信息。
8. API 文档信息。
9. 认证信息。
10. 鉴权信息。
11. 角色信息。
12. 租户信息。
13. 第三方服务信息。
14. 云资产信息。
15. CI/CD 信息。
16. 部署信息。
17. 日志和监控信息。
18. 测试样例信息。
19. seed / mock / fixture 信息。
20. 隐藏接口信息。
21. 废弃接口信息。
22. source map 信息。
23. service worker 信息。
24. GraphQL / WebSocket / RPC 信息。
25. 依赖与供应链信息。
26. 业务对象与敏感数据流信息。
27. 权限边界与攻击面图谱信息。

任何没有检查到的维度，都必须补充为新的审计项。

三、检查是否存在“看起来很强但实际不可执行”的问题

逐项审查：

1. 是否只是 md 写得强，但没有脚本。
2. 是否有脚本但没有入口。
3. 是否有入口但没有参数说明。
4. 是否有参数但没有错误处理。
5. 是否有输出但不是结构化 JSON。
6. 是否有 JSON 但没有 schema。
7. 是否有 schema 但没有校验器。
8. 是否有校验器但没有测试样例。
9. 是否有测试样例但没有真实项目回放。
10. 是否有报告模板但没有 evidence 映射。
11. 是否有信息收集清单但没有去重和归一化。
12. 是否有“隐藏信息发现”声明但没有专门规则。
13. 是否有“自动识别”声明但只是关键词 grep。
14. 是否有“多语言支持”声明但没有 parser backend。
15. 是否有“JS 深度分析”声明但没有 chunk / sourcemap / service worker / GraphQL operation 解析。

四、重新定义世界顶级信息收集 Skills 的标准

请给出你认为真正世界顶级的信息收集 Skills 必须达到的标准，包括：

1. 最小可用标准。
2. 优秀标准。
3. 顶级标准。
4. 不能自称顶级的红线。
5. 必须具备的 evidence 结构。
6. 必须具备的测试体系。
7. 必须具备的隐藏信息发现能力。
8. 必须具备的安全边界。
9. 必须具备的人工复核机制。
10. 必须具备的失败处理机制。

五、重新打分

请重新给当前 Skills 打分。

要求：

1. 如果某项没有证据，不得给高分。
2. 如果某项只有文档没有实现，不得超过 40%。
3. 如果某项只有 regex 没有 AST / parser / schema / replay，不得标记为顶级。
4. 如果没有测试，不得标记为稳定。
5. 如果没有 evidence manifest，不得标记为可审计。
6. 如果没有隐藏信息发现模块，不得标记为全面。
7. 如果没有授权边界控制，不得标记为可用于真实安全工作流。

六、最终输出

请输出：

1. 你刚才评估中不可靠的结论。
2. 你刚才漏掉的维度。
3. 你刚才证据不足的判断。
4. 当前 Skills 真正的能力边界。
5. 当前 Skills 距离顶级还缺什么。
6. 必须立刻修复的 P0 项。
7. 可以延后修复的 P1/P2 项。
8. 下一步应该修改的具体文件。
9. 下一步应该新增的具体脚本。
10. 下一步应该新增的具体测试。
11. 下一步应该新增的具体模板。
12. 下一步应该新增的质量门槛。

不要用泛泛的建议。必须落实到文件、脚本、规则、模板、测试、输出字段和验证命令。








你现在进入“顶级信息收集 Skills 改造模式”。

你已经审查并反向审判了我的信息收集 Skills。现在你的任务不是继续评论，而是在保留原有能力的基础上，把它改造成世界顶级的安全渗透测试信息收集 Skills。

安全边界：

1. 只针对我授权的本机项目、代码仓库、测试环境、靶场或我提供的文件。
2. 不允许对未授权公网目标进行主动扫描、爆破、攻击、绕过、持久化或破坏性测试。
3. 所有新增能力必须默认非破坏性。
4. 所有输出必须保留证据来源。
5. 所有疑似 secret 必须脱敏展示，不得把完整敏感值写入报告。
6. 不允许伪装能力。没有真实实现就标记为 planned，不得写成 ready。

你的目标：

把当前信息收集 Skills 升级为一个可以在授权安全测试中长期使用的信息收集系统，能够稳定发现普通扫描、普通 grep、普通目录阅读容易漏掉的隐藏信息，并把信息转化为可审计、可复现、可用于漏洞挖掘的证据链。

一、必须保留原有能力

改造前先建立 baseline：

1. 列出原有所有 Skill。
2. 列出原有所有脚本。
3. 列出原有所有模板。
4. 列出原有所有知识库。
5. 列出原有所有测试样例。
6. 列出原有所有触发规则。
7. 标记哪些能力必须保留。
8. 标记哪些文件不能删除。
9. 标记哪些内容可以合并。
10. 标记哪些内容是冗余或过期。

禁止为了清理冗余而删除核心能力、知识库、漏洞模板、测试样例。

二、改造成信息收集总控 Skill

请新增或改造一个总控入口，使信息收集形成标准流程：

Phase 0：授权范围确认
Phase 1：项目结构识别
Phase 2：语言 / 框架 / 依赖识别
Phase 3：路由 / API / 入口点识别
Phase 4：认证 / 鉴权 / 角色 / 租户识别
Phase 5：配置 / 部署 / CI/CD / 云资产识别
Phase 6：前端 JS / sourcemap / chunk / service worker / manifest 识别
Phase 7：隐藏信息发现
Phase 8：敏感数据与危险操作入口识别
Phase 9：攻击面知识图谱生成
Phase 10：证据归档与报告输出
Phase 11：质量门槛检查
Phase 12：人工复核队列输出

每个 Phase 必须有：

1. 触发条件。
2. 输入。
3. 输出。
4. 调用脚本。
5. 证据字段。
6. 失败处理。
7. 质量门槛。
8. 测试样例。

三、新增或强化脚本能力

请根据现有结构新增或改造以下脚本。具体文件名可以根据项目结构调整，但必须输出最终路径：

1. scope_guard.py
   用于校验授权范围，阻止越界路径和未授权目标。

2. project_fingerprint.py
   用于识别语言、框架、包管理器、构建工具、运行时、目录结构。

3. route_api_extractor.py
   用于提取后端路由、前端路由、API 文档、OpenAPI、Swagger、Postman collection、GraphQL、WebSocket、RPC 入口。

4. auth_boundary_collector.py
   用于提取认证入口、鉴权中间件、角色、权限、租户字段、组织字段、owner 字段、admin 字段。

5. config_secret_signal_collector.py
   用于识别 env、config、docker、k8s、terraform、CI/CD、日志、监控、第三方服务和 secret 名称线索。必须脱敏。

6. js_deep_info_collector.py
   用于识别 JS bundle、chunk、source map、service worker、manifest、前端隐藏 API、GraphQL operation、feature flag、测试开关。

7. hidden_info_collector.py
   用于识别注释、旧接口、废弃文档、备份文件、mock、seed、fixture、migration、邮件模板、短信模板、通知模板、反向代理隐藏路径。

8. dependency_surface_collector.py
   用于识别危险依赖、供应链风险线索、构建脚本、postinstall、插件、loader、动态 import。

9. evidence_manifest_builder.py
   用于把所有信息收集结果统一写入 evidence manifest。

10. attack_surface_graph_builder.py
    用于生成 Asset → Endpoint → Auth → Role → Tenant → Parameter → Data Object → Sensitive Operation → Evidence 的知识图谱。

11. info_quality_gate.py
    用于检查信息收集是否达到最低质量门槛。

12. human_review_queue.py
    用于输出需要人工复核的信息，例如可疑 secret、疑似隐藏接口、疑似越权入口、疑似租户边界。

每个脚本必须支持：

1. --input
2. --output
3. --scope
4. --format json
5. --dry-run
6. --no-network
7. --redact-secrets
8. --max-files
9. --timeout
10. --verbose

四、统一 evidence manifest

请设计或改造 evidence manifest schema，至少包含：

1. evidence_id
2. collector_name
3. skill_name
4. source_file
5. source_line_start
6. source_line_end
7. source_type
8. discovered_item_type
9. discovered_item_value_redacted
10. raw_value_hash
11. confidence
12. severity_hint
13. auth_relevance
14. tenant_relevance
15. role_relevance
16. endpoint_relevance
17. data_sensitivity
18. reproduction_hint
19. collection_time
20. scope_id
21. false_positive_reason
22. needs_human_review
23. linked_report_section

五、增强隐藏信息发现规则

请新增规则文件，至少覆盖：

1. JS 隐藏接口。
2. source map 源码泄露。
3. service worker 缓存接口。
4. GraphQL operation。
5. WebSocket event。
6. OpenAPI 未使用接口。
7. 后端未暴露到前端的接口。
8. 测试账号线索。
9. 默认管理员线索。
10. seed 数据线索。
11. mock 数据线索。
12. migration 中的敏感字段。
13. CI/CD 部署域名。
14. Docker/K8s 内部服务名。
15. Terraform 云资产。
16. OAuth redirect_uri。
17. CORS allowlist。
18. CSP connect-src。
19. S3/OSS/COS bucket 名称。
20. 邮件/短信模板隐藏链接。
21. 反向代理隐藏路径。
22. 注释中的旧接口。
23. 备份文件与临时文件。
24. debug/dev/test/staging 环境差异。
25. 角色、租户、组织、owner、admin、permission、scope、policy 关键词族。

六、增强报告模板

请新增或改造信息收集报告模板，必须输出：

1. 授权范围。
2. 项目指纹。
3. 技术栈。
4. 入口点总览。
5. 路由/API 清单。
6. 前端 JS 信息。
7. 配置与部署信息。
8. 认证与鉴权信息。
9. 角色与租户信息。
10. 第三方服务信息。
11. 隐藏信息清单。
12. 高价值攻击面。
13. 需要人工复核的信息。
14. 已脱敏敏感信息。
15. 信息收集质量评分。
16. 未覆盖区域。
17. 下一步漏洞挖掘建议。
18. 证据索引。

七、测试体系

请新增测试样例，至少包含：

1. 正常项目样例。
2. 前后端混合项目样例。
3. Express / Next.js 样例。
4. Django / FastAPI 样例。
5. Spring / Laravel 样例。
6. Go / Rust 样例。
7. 有 source map 的前端样例。
8. 有 service worker 的样例。
9. 有 GraphQL 的样例。
10. 有 WebSocket 的样例。
11. 有 Docker/K8s/CI 的样例。
12. 有 seed/mock/fixture 的样例。
13. 有 hidden API 的样例。
14. 有 false positive 的负样本。
15. 有越界 scope 的阻断样本。

每个测试必须验证：

1. 是否收集到目标信息。
2. 是否没有越界。
3. 是否输出 evidence manifest。
4. 是否脱敏。
5. 是否进入质量门槛。
6. 是否需要人工复核。

八、改造输出要求

你必须输出：

1. 修改了哪些文件。
2. 新增了哪些文件。
3. 删除或合并了哪些冗余文件。
4. 保留了哪些原有能力。
5. 新增了哪些信息收集能力。
6. 新增了哪些隐藏信息发现规则。
7. 新增了哪些测试样例。
8. 新增了哪些报告模板。
9. 新增了哪些质量门槛。
10. 如何运行。
11. 如何验证。
12. 当前仍未完成的能力。
13. 哪些能力只能标记为 planned，不能标记为 ready。

九、最终验收标准

改造完成后，请运行或给出可运行的验证命令：

1. 目录结构检查。
2. schema 校验。
3. 脚本 dry-run。
4. 单元测试。
5. fixture replay。
6. evidence manifest 生成。
7. 信息收集质量门槛。
8. 报告生成。
9. 越界 scope 阻断测试。
10. secret 脱敏测试。

最后给出新的评分，并说明它距离世界顶级信息收集 Skills 还差什么。



