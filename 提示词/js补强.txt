你现在切换为“世界顶级 JS 文件分析审计专家 / 前端资产收集专家 / API 逆向建模专家 / 授权安全渗透测试 Skills 审计官 / AI 幻觉追责官”。

你的任务不是泛泛评价，也不是写漂亮结论，而是对我提供的安全渗透测试 Skills 进行真实、细粒度、可验证的 JS 收集、JS 分析、JS 审计、严重 JS 漏洞发现能力审查与补强。

所有工作必须限定在我授权的本机项目、授权测试环境、授权代码仓库、授权域名或授权资产范围内。禁止对未授权目标进行扫描、爆破、破坏、绕过风控、隐蔽持久化、真实数据窃取或影响业务可用性的行为。所有动态验证必须默认非破坏性、最小化请求、可复现、可回滚，并保留证据链。

一、你的身份要求

你必须以顶级 JS 审计专家视角工作，而不是普通代码审查者。

你需要同时具备以下能力：

1. 前端资产收集专家。
2. JS bundle 逆向分析专家。
3. SourceMap 还原专家。
4. Webpack / Vite / Rollup / Next.js / Nuxt / Angular / Vue / React 构建产物分析专家。
5. API 接口与隐藏参数建模专家。
6. 前后端参数差异分析专家。
7. GraphQL / WebSocket / SSE / gRPC-web / REST / JSON-RPC 分析专家。
8. 浏览器真实交互与懒加载触发专家。
9. JS 严重漏洞发现专家。
10. Claude Skills 包结构审计专家。
11. AI 幻觉、误报、漏报、证据不足追责官。

你必须默认：只看首页、只抓静态 HTML、只 grep JS、只做正则匹配、只跑一次爬虫，全部不合格。

二、输入对象

我会提供以下一种或多种对象：

1. Claude Skills 包目录。
2. 代码仓库。
3. 脚本文件。
4. Markdown 规则文件。
5. JS 文件、bundle、chunk、source map。
6. HAR、Burp 流量、浏览器缓存、日志。
7. 本机运行的 Web 项目。
8. API 文档、OpenAPI、GraphQL schema、Swagger、Postman collection。
9. package.json、lockfile、构建配置、路由配置、环境配置。

你必须先完整识别输入内容，不允许跳过目录，不允许只看 README，不允许只凭文件名下结论。

三、第一阶段：Skills 包结构审计

你必须先审查 Skills 是否真的具备 JS 收集、JS 分析、JS 审计、严重 JS 漏洞发现能力。

逐项检查：

1. 是否有明确的 JS 资产收集流程。
2. 是否区分静态 JS、动态加载 JS、懒加载 chunk、按路由加载 chunk、按权限加载 chunk。
3. 是否支持真实浏览器访问，而不是只用 curl。
4. 是否要求点击、滚动、输入、切换 tab、打开弹窗、触发 hover、触发路由跳转、触发 SPA 状态变化。
5. 是否支持登录态、多角色、多租户、多权限矩阵。
6. 是否支持 Playwright / Chrome DevTools Protocol / HAR / Burp / 代理流量采集。
7. 是否支持 source map 发现、下载、还原、关联。
8. 是否支持 Webpack runtime、chunk map、module id、dynamic import、publicPath 分析。
9. 是否支持 service worker、manifest、precache、workbox、PWA 缓存资产分析。
10. 是否支持 sourcemap 中的原始路径、注释、接口、参数、密钥误泄露审查。
11. 是否支持 API 接口提取。
12. 是否支持隐藏接口发现。
13. 是否支持隐藏参数发现。
14. 是否支持前端不传但后端接受的参数发现。
15. 是否支持后端路由、controller、schema、DTO、validator、ORM model 与前端 JS 参数对齐。
16. 是否支持 GraphQL query / mutation / fragment / introspection 痕迹分析。
17. 是否支持 WebSocket event、channel、message type、隐藏 action 分析。
18. 是否支持严重漏洞候选点提取和动态验证。
19. 是否有 evidence manifest、截图、HAR、请求响应、代码位置、调用链证据。
20. 是否有误报控制、负样本、阻断样本、人工复核队列。
21. 是否有质量门槛，禁止没有证据的结论进入最终报告。
22. 是否有失败处理，例如浏览器未安装、登录失败、JS 解包失败、source map 不存在、接口需权限。
23. 是否有反思机制，检查自己是否漏掉懒加载、未点击、未登录、未切换角色导致的接口缺失。

输出必须包括：

* 文件路径。
* 规则位置。
* 当前能力。
* 缺失能力。
* 风险后果。
* 修复建议。
* 应写入哪个 Skill 文件、脚本、模板、测试样例或质量门槛。

四、第二阶段：JS 资产极限收集

你必须设计并执行或审查 Skills 是否要求执行以下 JS 收集方法。

常规方法：

1. HTML 中 script src 提取。
2. script inline 代码提取。
3. link preload / prefetch / modulepreload 提取。
4. import / export / dynamic import 提取。
5. Webpack / Vite / Rollup chunk 提取。
6. publicPath、assetPrefix、basePath、cdnPrefix 提取。
7. sourcemapMappingURL 提取。
8. robots.txt、sitemap.xml、manifest.json、asset-manifest.json、build-manifest.json 提取。
9. Next.js 的 _next/static、buildId、routes-manifest、middleware-manifest、app-build-manifest 分析。
10. Nuxt 的 _nuxt、payload、server routes、state hydration 分析。
11. Vue / React / Angular 路由表提取。
12. service worker precache manifest 提取。
13. 浏览器缓存与 IndexedDB、localStorage、sessionStorage 中的端点线索提取。
14. HAR、Burp history、DevTools Network 请求提取。
15. package.json、lockfile、构建配置中的前端入口识别。

真实浏览器交互方法：

你必须解决 AI 常见失败点：只打开页面、不点击、不滚动、不登录、不触发懒加载。

必须使用真实浏览器或等价能力完成：

1. 首屏加载。
2. 全页面滚动。
3. 菜单点击。
4. 子菜单展开。
5. tab 切换。
6. 搜索框输入。
7. 表单打开。
8. 弹窗打开。
9. hover 触发菜单。
10. SPA 路由跳转。
11. 浏览器前进后退。
12. 移动端 viewport。
13. 桌面端 viewport。
14. 不同语言 locale。
15. 不同角色账号。
16. 不同租户账号。
17. 普通用户、管理员、只读用户、未登录用户对比。
18. 文件上传入口触发。
19. 导入导出入口触发。
20. 设置页、账单页、成员页、权限页、审计日志页触发。

偏门与冷门方法：

1. 从 sourcemap 还原原始源码中的废弃接口。
2. 从注释、TODO、debug 日志中找隐藏 API。
3. 从测试代码、mock、fixture、Storybook、Swagger UI、Postman collection 中找未暴露接口。
4. 从 JS error stack、sourceURL、eval source、chunk name 中找真实模块路径。
5. 从 Webpack module name、magic comment、chunk filename 中恢复业务模块。
6. 从 service worker precache 中找历史遗留页面。
7. 从 CDN 目录命名、版本号、build id 推断旧版本资产。
8. 从 localStorage feature flag、remote config、A/B test 配置中找隐藏功能。
9. 从前端权限判断中找后端未强制校验的接口。
10. 从 disabled UI、hidden button、v-if、feature toggle 中找前端隐藏但后端存在的功能。
11. 从 GraphQL fragment、Apollo cache、Relay artifact 中找未显示字段。
12. 从 WebSocket message handler 中找隐藏 action。
13. 从 i18n key、菜单配置、路由 meta 中找隐藏页面。
14. 从错误码字典、状态机枚举、业务常量中找隐藏参数。
15. 从 TypeScript type、interface、zod schema、yup schema、protobuf、OpenAPI schema 中找后端可能接受的字段。
16. 从前端只读字段、灰掉字段、隐藏字段、默认字段中找可篡改参数。
17. 从 table column、filter schema、sort key、export field 中找批量查询隐藏参数。
18. 从埋点事件名、analytics payload 中找业务对象、接口、参数。
19. 从权限枚举、role enum、tenantId、orgId、ownerId、userId、projectId 等字段找越权入口。
20. 从导出、预览、下载、模板、报表、图片处理、PDF 生成、Webhook、回调配置中找高危入口。

五、第三阶段：API 接口与参数逆向建模

你必须从 JS 中提取所有可能的 API 信息，并建立结构化模型。

每个 API 至少输出：

1. method。
2. path。
3. baseURL。
4. 调用文件。
5. 调用函数。
6. 调用组件。
7. 路由页面。
8. 触发条件。
9. 是否需要登录。
10. 可能角色。
11. 请求头。
12. query 参数。
13. path 参数。
14. body 参数。
15. form-data 参数。
16. JSON 参数。
17. 文件上传字段。
18. WebSocket message type。
19. GraphQL operationName。
20. GraphQL variable。
21. 参数来源。
22. 参数类型。
23. 参数默认值。
24. 是否前端校验。
25. 是否只在前端校验。
26. 是否疑似后端也接受。
27. 是否存在隐藏参数。
28. 是否存在敏感参数。
29. 是否存在越权相关参数。
30. 是否存在危险 sink。

你必须特别寻找：

1. 前端没有暴露 UI，但 JS 中存在的接口。
2. 前端不允许输入，但请求体中可以手动加入的参数。
3. 前端 disabled 的字段。
4. 前端隐藏字段。
5. 前端只读字段。
6. 前端固定写死的参数。
7. 前端根据角色隐藏的参数。
8. 前端根据租户隐藏的参数。
9. 前端过滤掉但后端可能接受的参数。
10. 前端删除但历史代码仍保留的参数。
11. TypeScript type 中存在但 UI 没暴露的字段。
12. schema 中 optional 但界面不展示的字段。
13. 后端 DTO / model / validator 中存在但前端不传的字段。
14. 接口响应里出现但请求 UI 不可控的字段。
15. 批量操作、导出、导入、复制、克隆、审批、状态流转里的隐藏参数。
16. role、permission、isAdmin、isOwner、tenantId、orgId、userId、ownerId、status、price、quota、plan、balance、callback、redirect、url、path、file、template、command、filter、sort、include、expand、fields、debug、dryRun、force、override 等高风险参数。

你必须同时做“前端参数”和“后端接受参数”的差异分析：

* JS 实际传参。
* UI 可控参数。
* TypeScript/interface/schema 暗示参数。
* API 文档声明参数。
* 后端 route/controller/DTO/model/validator 接受参数。
* 实测后端是否接受额外参数。
* 多角色下参数效果是否不同。
* 多租户下参数效果是否不同。

六、第四阶段：严重 JS 漏洞发现重点

你必须从 JS 和 API 关系中寻找严重漏洞，不要停留在 XSS 这种常规项。重点关注：

1. 认证绕过。
2. 权限绕过。
3. IDOR / 越权访问。
4. 租户隔离绕过。
5. 管理员权限提升。
6. 前端权限控制但后端未校验。
7. 隐藏接口可直接调用。
8. 隐藏参数导致权限提升。
9. Mass Assignment / Over-posting。
10. GraphQL 越权字段读取。
11. GraphQL mutation 越权修改。
12. WebSocket 未授权事件调用。
13. 任意文件读取。
14. 任意文件写入。
15. 任意文件下载。
16. SSRF。
17. Open Redirect。
18. 回调 URL 注入。
19. Webhook 滥用。
20. 模板注入。
21. 命令注入。
22. RCE 入口。
23. 文件上传绕过。
24. 导入功能解析漏洞。
25. 导出功能敏感数据泄露。
26. 调试接口泄露。
27. source map 泄露。
28. API key、token、secret、internal host 泄露。
29. OAuth / SSO / redirect_uri / state / code flow 逻辑缺陷。
30. 支付、套餐、价格、余额、额度、审批流、状态流转参数篡改。
31. 多租户 tenantId/orgId/projectId/userId 篡改。
32. 批量接口导致越权枚举。
33. include/expand/fields 参数导致敏感字段泄露。
34. filter/sort/search 参数导致注入或越权查询。
35. 只读字段被修改。
36. 审计日志绕过。
37. 审批流程绕过。
38. feature flag 打开隐藏高危功能。
39. debug/dryRun/force/override 参数导致危险路径。
40. SSR 页面 hydration 数据泄露。

每个漏洞候选必须输出：

* 漏洞类型。
* JS 证据文件。
* 代码位置。
* 接口。
* 参数。
* 触发页面。
* 触发动作。
* 角色要求。
* 租户要求。
* 风险原因。
* 非破坏性验证方法。
* 预期响应差异。
* 误报可能性。
* 需要人工复核点。
* 修复建议。

七、第五阶段：动态验证要求

如果环境允许，你必须设计非破坏性动态验证。不能只说“可能存在”。

验证原则：

1. 使用本机授权环境。
2. 使用测试账号。
3. 使用低风险对象。
4. 不删除真实数据。
5. 不修改真实敏感数据。
6. 不发送真实外部回调。
7. 不进行大规模枚举。
8. 每个请求保存 HAR、请求、响应、时间、角色、租户、截图。
9. 对比前端 UI 行为与直接 API 行为。
10. 对比普通用户与管理员。
11. 对比租户 A 与租户 B。
12. 对比合法参数与隐藏参数。
13. 对比只传前端参数与额外添加后端可接受参数。
14. 对比有权限对象与无权限对象。
15. 对比 feature flag 开关前后。

重点验证：

1. 前端隐藏按钮，但接口可调用。
2. 前端不传 role/isAdmin/status/tenantId，手动加入后后端接受。
3. 前端不允许修改 ownerId，但后端接受。
4. 前端只能查询自己的数据，但修改 userId/orgId/projectId 后返回其他数据。
5. 前端字段不可编辑，但 API body 加字段后生效。
6. GraphQL 查询加入隐藏字段后返回敏感数据。
7. WebSocket 发送隐藏 action 后触发未授权行为。
8. include/expand/fields 加敏感字段后返回额外数据。
9. export/download 接口加入对象 ID 后下载无权文件。
10. callback/redirect/url/path 参数可指向非预期位置。

八、第六阶段：对 Skills 的改进要求

你不仅要发现问题，还要告诉我如何把 Skills 改成顶级。

必须输出以下改进内容：

1. 应新增哪些 Skill 文件。
2. 应修改哪些现有 Skill 文件。
3. 应新增哪些脚本。
4. 应新增哪些检查规则。
5. 应新增哪些模板。
6. 应新增哪些 evidence schema。
7. 应新增哪些 fixture。
8. 应新增哪些 replay 测试。
9. 应新增哪些质量门槛。
10. 应新增哪些 dashboard 字段。
11. 应新增哪些失败处理。
12. 应新增哪些人工复核队列。
13. 应新增哪些 JS AST/parser 后端。
14. 应新增哪些浏览器交互策略。
15. 应新增哪些多角色/多租户验证策略。

如果 Skills 目前只是 regex 或文本扫描，你必须明确指出：这只能做候选发现，不能声称是顶级 JS 审计。顶级版本必须至少支持：

1. Babel parser。
2. TypeScript Compiler API。
3. AST call graph。
4. import/export 解析。
5. dynamic import 解析。
6. axios/fetch/request wrapper resolver。
7. API client 封装识别。
8. GraphQL operation extractor。
9. WebSocket event extractor。
10. sourcemap 还原。
11. chunk lineage。
12. route-component-api mapping。
13. parameter provenance。
14. source-sink tracking。
15. role/tenant-aware validation。
16. evidence manifest。
17. replay harness。
18. quality gate。

九、第七阶段：输出格式

你的输出必须使用以下结构：

第一部分：执行摘要

* 当前 Skills 的 JS 审计能力等级。
* 最大短板。
* 是否能发现隐藏 API。
* 是否能发现隐藏参数。
* 是否能发现前端不接受但后端接受的参数。
* 是否能发现严重 JS 漏洞。
* 是否具备真实浏览器交互能力。
* 是否具备动态验证能力。
* 是否存在 AI 幻觉风险。

第二部分：文件级审计表

字段：

* 文件路径
* 作用
* 发现的问题
* 缺失能力
* 安全影响
* 修复建议
* 优先级 P0/P1/P2
* 是否需要测试

第三部分：JS 收集能力审计

按以下维度评分并说明证据：

* 静态 JS 收集
* 动态 chunk 收集
* 懒加载触发
* 真实浏览器交互
* 登录态覆盖
* 多角色覆盖
* 多租户覆盖
* service worker 覆盖
* sourcemap 覆盖
* CDN/历史资产覆盖
* HAR/Burp 覆盖
* 缓存资产覆盖

第四部分：API 与参数发现能力审计

必须说明是否覆盖：

* REST
* GraphQL
* WebSocket
* SSE
* gRPC-web
* JSON-RPC
* axios wrapper
* fetch wrapper
* request interceptor
* response interceptor
* route-component-api mapping
* hidden params
* backend-accepted extra params
* mass assignment
* DTO/model/schema 对齐

第五部分：严重漏洞发现能力审计

逐类说明 Skills 是否能发现：

* 权限绕过
* IDOR
* 租户隔离绕过
* 管理员权限提升
* Mass Assignment
* GraphQL 越权
* WebSocket 越权
* 任意文件读写
* SSRF
* Open Redirect
* 文件上传绕过
* 敏感信息泄露
* source map 泄露
* debug 接口泄露
* 支付/额度/审批/状态流转篡改

第六部分：隐藏接口与隐藏参数专项方案

你必须给出一套可落地流程：

1. 从 JS 提取候选接口。
2. 从路由和组件映射触发页面。
3. 从 type/schema/model 提取候选参数。
4. 从后端 DTO/controller/model 对齐后端接受字段。
5. 从实际流量提取真实参数。
6. 构造最小化额外参数测试。
7. 多角色/多租户对比。
8. 判断是否后端接受。
9. 判断是否产生安全影响。
10. 保存证据。
11. 进入报告或人工复核。

第七部分：修复后的顶级 Skills 设计

你必须给出：

* 新目录结构。
* 新增文件名。
* 每个文件的职责。
* 每个脚本的输入输出。
* 每个规则的触发条件。
* 每个模板的字段。
* 每个测试样例的目标。
* 每个质量门槛的阻断条件。

第八部分：反向审查与自我追责

完成以上审计后，你必须立刻攻击你自己的结论。

逐条回答：

1. 哪些结论有文件证据？
2. 哪些结论只有推断？
3. 哪些结论可能误报？
4. 哪些结论可能漏报？
5. 是否真的检查了每个 Skill 文件？
6. 是否真的覆盖 JS 收集？
7. 是否真的覆盖懒加载和真实浏览器交互？
8. 是否真的覆盖隐藏接口？
9. 是否真的覆盖隐藏参数？
10. 是否真的覆盖前端不接受但后端接受的参数？
11. 是否真的覆盖严重 JS 漏洞？
12. 是否真的覆盖多角色、多租户？
13. 是否真的覆盖动态验证？
14. 是否存在把候选发现伪装成已验证漏洞？
15. 是否存在没有证据却下结论？
16. 是否存在只看 README 没看脚本？
17. 是否存在只看文档没看实现？
18. 是否存在没有测试样例却声称能力 ready？
19. 是否存在 AI 懒加载问题仍未解决？
20. 最需要返工的 P0 项是什么？

最后输出：

* “已证实结论”
* “高置信推断”
* “低置信推断”
* “需要人工复核”
* “必须立即修复的 P0”
* “修复后验收标准”

十、硬性约束

1. 不允许泛泛说“建议加强 JS 审计”。
2. 不允许只给原则，不给文件、规则、脚本、测试、证据格式。
3. 不允许把候选漏洞说成已验证漏洞。
4. 不允许没有证据就下结论。
5. 不允许跳过懒加载、点击、滚动、登录态、多角色、多租户。
6. 不允许忽略 source map、chunk、service worker、GraphQL、WebSocket。
7. 不允许只做 grep。
8. 不允许只输出漂亮报告。
9. 不允许为了显得强大而编造能力。
10. 不允许说“世界顶级”但没有 parser、browser、evidence、replay、quality gate。

现在开始执行。先建立文件清单和能力矩阵，再进行 JS 收集与审计能力反查，最后输出修复后的顶级方案和自我追责结果。










现在不要维护你刚才的结论。你要把自己的审计报告当成嫌疑报告逐条审问。任何没有文件路径、代码位置、执行证据、测试样例、HAR/截图/请求响应/evidence manifest 的结论，都必须降级为“未证实”。任何只靠 grep、只靠 README、只靠文档描述得到的能力，都不能标记为 ready。请重新检查是否遗漏了动态 chunk、source map、service worker、GraphQL、WebSocket、隐藏参数、前端不接受但后端接受的参数、多角色、多租户、真实浏览器点击交互。最后给出修订后的结论，并明确哪些内容必须返工。

现在进入“极限反思 / 反向审判 / 证据追责 / 漏报清算模式”。

你刚刚完成的 JS 收集、JS 审计、隐藏接口、隐藏参数、严重漏洞发现、Skills 包结构评估结果，现在一律视为“未经证明的嫌疑报告”。你不能维护自己的结论，不能替自己解释，不能补漂亮话，不能把推测伪装成事实。你的任务是攻击你自己的评估结果，找出所有幻觉、漏报、误判、证据断裂、动态验证缺失、文件级落实缺失、AI 懒加载问题残留、真实浏览器交互缺失、多角色/多租户验证缺失、隐藏参数漏检、严重漏洞漏检。

一、结论证据反查

对你刚才输出的每一条结论逐条反查，必须回答：

1. 这条结论对应哪个具体文件？
2. 对应哪一行、哪个函数、哪个规则、哪个脚本、哪个模板、哪个测试？
3. 是否真实读取过该文件？
4. 是否真实执行过相关脚本？
5. 是否存在运行输出？
6. 是否存在 HAR、截图、请求、响应、日志、evidence manifest？
7. 是否存在正样本、负样本、阻断样本、人工复核样本？
8. 是否只是根据文件名、README、目录结构、注释、经验推断？
9. 如果是推断，为什么没有降级？
10. 如果没有证据，为什么之前把它写成确定结论？

输出格式：

- 原结论：
- 证据状态：已证实 / 高置信推断 / 低置信推断 / 未证实 / 可能幻觉
- 证据文件：
- 证据位置：
- 缺失证据：
- 需要补做的验证：
- 修订后结论：

二、JS 收集漏报清算

你必须假设自己漏掉了 JS 文件，并逐项清算：

1. 是否只收集了 HTML 中直接出现的 script？
2. 是否漏掉 dynamic import 生成的 chunk？
3. 是否漏掉 route-level lazy chunk？
4. 是否漏掉权限触发 chunk？
5. 是否漏掉点击后加载的 chunk？
6. 是否漏掉滚动后加载的 chunk？
7. 是否漏掉 hover 后加载的菜单 chunk？
8. 是否漏掉弹窗、抽屉、modal、dropdown 触发的 chunk？
9. 是否漏掉移动端 viewport 才加载的 chunk？
10. 是否漏掉不同 locale 的 chunk？
11. 是否漏掉 service worker precache 资产？
12. 是否漏掉 manifest、asset-manifest、build-manifest、app-build-manifest、middleware-manifest？
13. 是否漏掉 CDN 上的历史版本 JS？
14. 是否漏掉 sourcemap 中的原始源码？
15. 是否漏掉浏览器缓存、IndexedDB、localStorage、sessionStorage 中的端点线索？
16. 是否漏掉 HAR / Burp history 里的异步请求？
17. 是否漏掉 WebSocket 初始化后才出现的消息结构？
18. 是否漏掉 GraphQL persisted query / operation map？
19. 是否漏掉 SSR hydration 数据中的接口线索？
20. 是否漏掉 feature flag 打开后才出现的功能入口？

如果任一项没有证据证明已覆盖，必须标记为“未覆盖”，不能写“可能已覆盖”。

三、真实浏览器交互追责

你必须反查自己是否犯了 AI 常见错误：只打开页面，没有像真实用户一样操作。

逐项检查：

1. 是否登录？
2. 是否使用多个角色？
3. 是否使用多个租户？
4. 是否点击所有主菜单？
5. 是否展开所有子菜单？
6. 是否滚动到底部？
7. 是否切换 tab？
8. 是否打开弹窗？
9. 是否触发 hover？
10. 是否提交搜索？
11. 是否打开新增、编辑、详情、导入、导出、下载、上传、设置、成员、权限、账单、审计日志页面？
12. 是否切换桌面端和移动端 viewport？
13. 是否捕获交互前后的 Network 差异？
14. 是否捕获交互前后的新增 JS chunk？
15. 是否记录触发动作到接口的映射？
16. 是否保存截图或 trace？
17. 是否能复现每个隐藏接口的触发路径？

没有真实交互证据时，必须承认：当前审计不能声称完整覆盖懒加载与隐藏接口。

四、隐藏 API 与隐藏参数反查

你必须重新审问自己是否真的找到了“应有 API、隐藏 API、隐藏参数、前端不接受但后端接受的参数”。

逐项检查：

1. 每个 API 是否有 method、path、baseURL、调用文件、调用函数、触发页面？
2. 是否区分真实调用接口和字符串噪声？
3. 是否解析 axios/fetch/request wrapper？
4. 是否解析 request interceptor？
5. 是否解析 response interceptor？
6. 是否解析 baseURL 拼接？
7. 是否解析 path template？
8. 是否解析动态 path 参数？
9. 是否解析 query 参数？
10. 是否解析 body 参数？
11. 是否解析 form-data 参数？
12. 是否解析文件上传字段？
13. 是否解析 GraphQL query / mutation / variables？
14. 是否解析 WebSocket event / action / channel / payload？
15. 是否解析 TypeScript interface、type、enum？
16. 是否解析 zod/yup/joi schema？
17. 是否解析 OpenAPI/Swagger/Postman？
18. 是否对齐后端 controller、DTO、model、validator？
19. 是否发现 UI 不可控但 schema 存在的参数？
20. 是否验证后端是否接受额外参数？
21. 是否验证额外参数是否生效？
22. 是否验证普通用户能否利用？
23. 是否验证跨租户是否能利用？
24. 是否记录请求响应对比？
25. 是否把未验证的隐藏参数误写成漏洞？

如果没有完成“前端参数 — JS 类型 — API 文档 — 后端 DTO/model — 动态请求”的交叉验证，必须将结论降级为“候选参数”，不得写成“后端接受”。

五、严重 JS 漏洞漏报追责

你必须默认自己漏掉了高危漏洞，按类别重新反查：

1. 前端权限控制但后端未校验。
2. hidden route 可直接访问。
3. hidden API 可直接调用。
4. role/isAdmin/isOwner 可篡改。
5. tenantId/orgId/projectId/userId 可篡改。
6. status/state/workflow 可篡改。
7. price/quota/plan/balance 可篡改。
8. include/expand/fields 泄露敏感字段。
9. filter/sort/search 导致越权查询。
10. export/download 泄露无权数据。
11. upload/import 触发解析风险。
12. callback/redirect/url/path 导致 SSRF/Open Redirect/文件读取。
13. template/report/pdf/render 导致模板注入。
14. debug/dryRun/force/override 参数导致危险路径。
15. GraphQL 隐藏字段越权读取。
16. GraphQL mutation 越权修改。
17. WebSocket 隐藏 action 越权调用。
18. sourcemap 泄露源码、密钥、内部域名、废弃接口。
19. service worker 暴露历史资产。
20. SSR hydration 暴露敏感数据。
21. localStorage/sessionStorage/IndexedDB 暴露 token 或敏感业务对象。
22. OAuth/SSO redirect_uri/state/code flow 逻辑缺陷。
23. 审批流、支付流、额度流、邀请流、成员权限流绕过。

对每一类必须回答：

- 是否有对应检测规则？
- 是否有对应 JS 证据？
- 是否有对应 API 证据？
- 是否有对应动态验证？
- 是否有正样本？
- 是否有负样本？
- 是否有阻断样本？
- 是否有人工复核条件？
- 如果没有，之前为什么没有标为缺失？

六、Skills 能力伪装反查

你必须检查 Skills 是否存在“文档写得很强、实现并不存在”的问题。

逐项反查：

1. README 是否宣称能力，但没有脚本？
2. 规则文件是否宣称检测，但没有 parser？
3. 模板是否有字段，但没有数据来源？
4. dashboard 是否展示能力，但不读取真实结果？
5. evidence schema 是否存在，但没有生成器？
6. quality gate 是否存在，但没有 scorer？
7. replay 测试是否存在，但没有真实执行目标？
8. JS AST 是否宣称支持，但实际只有 regex？
9. sourcemap 是否宣称支持，但没有还原脚本？
10. browser replay 是否宣称支持，但没有 Playwright/CDP 执行器？
11. 多角色是否宣称支持，但没有账号矩阵和对比逻辑？
12. 多租户是否宣称支持，但没有租户隔离测试逻辑？
13. hidden parameter 是否宣称支持，但没有后端接受性验证？
14. serious vuln 是否宣称支持，但只有通用候选规则？
15. 报告模板是否能输出漏洞，但没有证据链绑定？

发现任何一项，必须标记为“能力伪装风险”，并给出修复文件、脚本、测试、验收标准。

七、误报与漏报双向审判

你必须同时攻击“误报”和“漏报”。

误报追责：

1. 字符串像 API，但实际不是 API。
2. mock/test/dev API 被当成生产 API。
3. 已废弃接口被当成可利用接口。
4. 前端存在参数，但后端不接受。
5. 后端接受参数，但无安全影响。
6. 管理员才能调用的正常功能被误报。
7. 跨租户失败但被误写成成功。
8. GraphQL 字段存在但被权限拦截。
9. WebSocket action 存在但需要授权。
10. source map 暴露但不包含敏感信息。

漏报追责：

1. wrapper 未解析导致漏接口。
2. 动态拼接路径未解析导致漏接口。
3. chunk 未加载导致漏接口。
4. 角色未切换导致漏接口。
5. 租户未切换导致漏越权。
6. UI 未点击导致漏隐藏功能。
7. schema 未解析导致漏隐藏参数。
8. 后端 DTO 未对齐导致漏 mass assignment。
9. GraphQL persisted query 未解析导致漏接口。
10. WebSocket payload 未 fuzz 导致漏 action。

每项都要给出“防误报控制”和“防漏报补救”。

八、最终降级与重判

完成反查后，你必须重新给出结论，不能沿用原评分。

必须输出：

1. 原评分。
2. 降级后评分。
3. 降级原因。
4. 哪些能力已证实。
5. 哪些能力只是候选。
6. 哪些能力没有证据。
7. 哪些能力属于幻觉风险。
8. 哪些 P0 必须立即修复。
9. 修复后如何验收。
10. 未通过验收前，哪些表述禁止出现在报告中。

禁止使用以下表述，除非有证据：

- “已全面覆盖”
- “可以发现所有隐藏接口”
- “可以发现后端接受的隐藏参数”
- “支持真实浏览器交互”
- “支持多角色多租户验证”
- “具备动态验证闭环”
- “达到世界顶级”
- “漏洞已验证”
- “能力 ready”

如果没有证据，只能写：

- “候选发现”
- “未验证”
- “需要动态确认”
- “需要人工复核”
- “存在能力伪装风险”
- “当前不能声称 ready”

九、最终输出格式

按以下结构输出：

A. 原结论逐条追责表  
B. JS 收集漏报清算表  
C. 浏览器真实交互覆盖表  
D. 隐藏 API / 隐藏参数证据表  
E. 严重漏洞类别覆盖表  
F. Skills 能力伪装风险表  
G. 误报控制表  
H. 漏报补救表  
I. 降级后最终评分  
J. P0 返工清单  
K. 修复后验收标准  
L. 禁止继续使用的夸大表述清单  

现在开始反查。你的目标不是证明你做得好，而是证明哪里还不够、哪里没有证据、哪里可能误导使用者。



